# ch06 专家评审记录（双专家深度复核）

> 设计侧溯源件（不交开发）。双专家：架构设计专家 + 产品管理专家。
> 复核对象：ch06 五份交付物（config / schema / PRD / 落地 / mockup）。
> 复核日期：2026-08-11。目的：确认设计合理、各文档一致、无 ch05 类冲突，可交付开发。

## 0. 总体结论

- **架构设计专家**：P0=0。字段契约一致性 OK；旧 schema 残留仅在「作废/修正」叙述中出现（无真实字段）；100 例数据流 OK；状态机/草稿契约 OK；下游/DRIFT OK；JSON + Draft7 schema OK。**可交付开发。**
- **产品管理专家**：P0=0。三栏布局、抽屉、关联擅长、合理性陷阱、文案一致性均 OK（无 ◎〇△ 串味）。唯一 P1 = 导出交付物错位（已设计侧修复）。**可交付。**

**双专家一致结论：设计层面无 P0，不会再有问题。剩余均为开发侧实现跟踪项（契约已定义）。**

---

## 1. 架构设计专家评审

### 逐项结论
1. **字段契约一致性 — OK**：chapter_id=`ch06`；step_role_id=psychologist/career_counselor（既有全局角色）；`reference_talents` 与 ch05 `reference_strengths` 同构（list_of_items→{text}）；`likes` 两态（step-1 text/locked/source、step-2 field/aspects/linked_strengths/sources/locked）在 config/PRD/落地一致；`passion_examples`（100 条，仅 id+text）在 config 与 schema 同名同结构；`mentor_hooks.references=[talents,likes]` 被 assembler 正确消费。
2. **旧 schema 残留 — OK**：`06-passion`/`passion-guide`/`passion-synthesizer`/`CREATE TABLE ch6_passion_list` 仅在落地 §9、PRD §6/§209/§378 作「作废/修正」叙述，无真实使用。
3. **100 例数据流 — OK**：`ui_context.passion_examples` 随 config 发布、前端读 config、不进 LLM prompt、引用写入 `likes.sources`。
4. **状态机与草稿契约 — OK**：`edit_output`(steps.py:401)、`submit_step`(steps.py:529) 全局 409；ch06 不在 `CHAPTER_UPSTREAM_REQUIREMENTS`，不被 `talents` 门禁（符合「ch5 可选」）。
5. **下游消费与 DRIFT — OK**：`likes` 已在 compaction 三重白名单（:16/34/39）；step-2 嵌套随 `likes` 落库；`check_config_consistency.py` CHAPTERS 含 ch06。
6. **契约测试覆盖 — P1**（开发侧）：① `reference_talents` 合并缺专属测试，assembler 无 ch06 step-2 显式合并块（对照 ch05 `_ch05_step2_candidates` assembler.py:574）；② 状态机 409 无 ch06 专项。
7. **JSON 合法性 & schema — OK**：ch06 通过 Draft7；ch02–ch06 全部通过。

### P1（开发侧，非设计矛盾）
- **A**：assembler.build_step_prompt 缺 ch06 step-2 `reference_talents` 显式合并块 → 补 `_ch06_step2_candidates` 镜像 ch05。
- **B**：steps.py 的 `edit_output`/`submit_step` 无 `_is_ch06_step` 分支与 `_validate_ch06_output`（commentary readonly、likes 嵌套、locked 布尔）后端不强制校验。建议补分支提升健壮性。

### P2（优化）
- C step-1 `source`(单) vs step-2 `sources`(复) 为有意双态，建议文档明示（已落地 §8.2 示例 + §9.9 说明）。
- D compaction 对 `likes` 保留 `locked`、对 `talents` 剥离 `locked`（compaction.py:60），建议对齐。
- E 落地 §0「assembler 完成 text→field 转换」措辞不准 → 已改为「LLM instruction 完成，assembler 仅拼装 prompt」（设计侧已修）。
- F 需敲定 `sources` 取值词表（q1–q5 与 mockup 的 100_examples/30_questions/self 并存）。

---

## 2. 产品管理专家评审

### 逐项结论
1. **三栏布局与原书对齐 — OK**（对齐 ch05）。缺口：中栏缺图 6-1 与达·芬奇引言 → 设计侧已在原型补中栏 图6-1 占位 + 引言（P2 已修）。
2. **100 例 / 30 问抽屉 — OK**：100 例引用正确标「引自100例」；30 问「思路」按钮仅触发提示（符合预期）。P2：30 问「去回答30问」按钮名暗示可作答（实为思路）；demo 仅 15 条（书为 30，题库待接）。
3. **关联擅长交互 — OK**：卡片「＋关联擅长」→抽屉选 ch5 长处→chip 清晰。P2：未演示 ch5 缺失降级态（开发侧按 spec 落地）。
4. **合理性陷阱呈现 — OK**：step-2 琥珀 banner + 中栏 POINT 清晰。P2：无对应「标来源为自我（有用驱动）」UI（属开发侧）。
5. **草稿保存 UX — 部分 OK**：step-1 有「已存 HH:MM」+ 提交态区分。P2：step-2 引用/编辑草稿无时间戳、无失败提示、无重入恢复演示（开发侧按落地 §8 落地，同 ch05）。
6. **导出/交付物 — P1（已修）**：原型原导出沿用 ch05《使用说明书》（文件名撞名、内容错位）→ 设计侧改为导出《喜欢的事清单》（`我的喜欢的事清单.md` + 领域×方面×关联擅长）。
7. **文案一致性 — OK（无 ◎〇△ 串味）**。P2：step-2 角色 chip 原为「心理咨询师」→ 设计侧已改为随步骤切「职业规划师」；运行按钮文案对齐落地 §7。
8. **需求覆盖度 — OK**：5 问/100 例/30 问/关联擅长/陷阱/锁定均覆盖，无自嗨功能。

### P1（设计侧已修）
- 导出交付物错位：原型导出由《使用说明书》改为《喜欢的事清单》，文件名与内容均区分。

### P2（开发侧为主）
- 中栏图 6-1 交互化（原型已占位，开发侧渲染三圆维恩图）。
- 30 问按钮名文案、题库接入、ch5 缺失降级态、摘要 Tab 填充、Step2 草稿时间戳/失败提示、整体导出 UI。

---

## 3. 设计侧修复清单（本轮已落地）

| # | 项 | 文件 | 说明 |
|---|---|---|---|
| 1 | 导出交付物 P1 | ch06 mockup | 《使用说明书》→《喜欢的事清单》（文件名+内容） |
| 2 | 角色 chip | ch06 mockup | step-2 切「职业规划师」 |
| 3 | 运行按钮文案 | ch06 mockup | 对齐落地 §7 |
| 4 | 图 6-1 + 引言 | ch06 mockup | 中栏补三圆占位 + 达·芬奇引言 |
| 5 | §0 保真措辞 | ch06 落地 | text→field 由 LLM instruction 完成 |

## 4. 开发侧待办（契约已定义，按落地 §10.2 落地）

assembler ch06 合并块（A）/ steps.py 校验分支（B）/ 契约测试 / 嵌套渲染器 `renderCh06StepBody` / 草稿真实持久化+失败提示 / 30 问题库 / ch5 降级态 / 清单整体导出 / reseed 消 DRIFT。
