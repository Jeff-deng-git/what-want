# ch07 专家评审记录（双专家深度复核）

> 设计侧溯源件（不交开发）。双专家：架构设计专家 + 产品管理专家。
> 复核对象：ch07 五份交付物（config / schema / PRD / 落地 / mockup）。
> 复核日期：2026-08-12。目的：确认设计合理、各文档一致、无 ch05 类冲突，可交付开发。

## 0. 总体结论

- **架构设计专家**：P0=2（均设计侧可修，已全部修复）；P1=2（均开发侧）；P2=1（开发侧）。字段契约一致性 OK；旧 schema 残留=0（仅在「作废」叙述出现）；JSON + Draft7 schema OK（ch02–ch07 全 PASS）；跨章注入机制经 `assembler.py:543` 实证。**修复后设计层无 P0，可交付开发。**
- **产品管理专家**：P0=0；P1=2（均已设计侧修复：两栏→三栏、补提交按钮）；P2=5（观察项，部分已随原型重建修复）。三栏布局、组合/筛选交互、分桶、提交态、导出均 OK。**可交付。**

**双专家一致结论：设计层面 P0 已全部修复（2 项均在设计侧），不会再有问题。剩余均为开发侧实现跟踪项（契约已定义）。**

---

## 1. 架构设计专家评审

### 逐项结论
1. **字段契约一致性 — OK（修复后）**：`chapter_id=ch07`；`step_role_id=career_counselor/mentor`（既有全局角色，未新增）；`ideal_works` item_schema `{title, like_source(只读), strength_source(只读), bucket(枚举), locked}` 在 config/PRD/落地一致；`mentor_hooks.references=[talents,likes,work_purpose,values]` 与 `step_context` 同键。
2. **跨章注入机制 — P0-A（已修）**：step-1 组合需读 ch5/ch6/ch4 上游进 step prompt，原稿仅放 `mentor_hooks`、`step_context` 留空 → step LLM 拿不到上游。实证 `assembler.py:543`（`profile_refs` 取自 `step_context`，`step_context` 存在时绝不回退 `mentor_hooks`）后，将 `talents/likes/work_purpose/values` 同时写入 `step_context.references` 与 `mentor_hooks.references`。**这是 ch07 相对 ch03/ch05「仅 mentor_hooks」惯例的首例扩展**，已在 config + 落地 §8.3 + §9#4 显式声明。
3. **孤儿字段 — P0-B（已修）**：原稿引用 `importance`，但 ch04 不 emit `importance`（属 ch03 早期假设），属孤儿键 → 去掉 `importance`，改 `values`（ch04 经 `top_values→values` 别名真实产出）。校验：config 不含 `importance`（grep=false），schema PASS。
4. **旧 schema 残留 — OK**：`intersection`/`success_statement`/`passions[]`/`purpose` 仅在落地 §8.1/§9、PRD 作「作废/修正」叙述，无真实使用；`ideal_works` 为独立新键（不覆盖 ch03 的 `intersection`）。
5. **JSON 合法性 & schema — OK**：ch07 通过 Draft7；ch02–ch07 全部通过。
6. **状态机与草稿契约 — OK（同构）**：`edit_output`(steps.py) submitted 态 409 + `submit_step`，与 ch02–ch06 同构；草稿隔离于 `user_answers`/draft run（落地 §8.6）。
7. **下游消费与 DRIFT — OK（开发侧待办）**：`ideal_works` 不在 compaction 白名单（新增一个已存在 `profile_json` 内的键，零新增表/列/迁移），需开发侧在三处白名单加入 + reseed；ch08 经 `mentor_hooks` 读 `ideal_works`。

### P0（设计侧已修，本轮）
- **A**：`step_context.references` 补 `[talents,likes,work_purpose,values]`（跨章注入 step prompt 关键）。文件：`chapter_config_ch07.json`。
- **B**：`importance` → `values`（去孤儿键）。文件：`chapter_config_ch07.json` / `PRD-ch7.md` / 落地 §5/§8.3。

### P1（开发侧）
- **A**：交接文档 §7.7 曾写旧稿（1 步 / `intersection` / references 缺 `values` / 无 `step_context` 行）→ 设计侧已翻新（本次，见 §3 #6）。
- **B**：ch07 缺前置 gate（校验 ch4/5/6 已提交）的后端分支 → steps.py 补 `_is_ch07_step` + `_validate_ch07_upstream`（对照 ch04 gating）。

### P2（优化，开发侧）
- C 契约测试缺口：assembler 缺 ch07 step-1 组合 / step-2 分桶显式合并块与级联测试；状态机 409 无 ch07 专项。

---

## 2. 产品管理专家评审

### 逐项结论
1. **三栏布局与原书对齐 — OK（已修 P1）**：原稿两栏降级 → 重建为「左步骤导航 / 中书籍原文 / 右 tab 面板」三栏（对齐 ch05/ch06），中栏填真实第七章原文 + 图7-2/7-3 占位。
2. **组合 / 筛选交互 — OK**：step-1 组合卡（来源标注 / 锁定）、step-2 筛选（工作目的 banner + 分桶过滤 + 下拉 + 下一步行动 textarea + 导出）齐备。
3. **提交态 UX — OK（已修 P1）**：原稿缺提交按钮 → 补「提交本步」+ 已提交徽章 / 锁定编辑 / 409 提示（对齐 ch05）。
4. **分桶与文案 — OK（部分 P2 已修）**：分桶标签加「（作为工作）」后缀；角色术语统一（「让职业规划师组合」）；导出含未归类项（未定项不丢弃）。
5. **需求覆盖度 — OK**：组合矩阵 / 工作目的过滤 / 螺旋上升 / 锁定 / 导出 / 导师回响均覆盖，无自嗨功能。

### P1（设计侧已修，本轮）
- 两栏 → 三栏布局。
- 缺提交按钮 → 补「提交本步」+ 提交态。

### P2（观察项，部分已随原型重建修复）
- 黑话：UI 文案个别处仍偏技术（bucket 枚举中文已在用，局部提示语待润色）。
- 未定项丢弃：已修（导出含未归类项）。
- 文案：step-1/step-2 按钮与提示语个别措辞待对齐落地 §7。
- 图7-3：中栏图7-3 仅占位，开发侧渲染。
- ch06 预告偏差：ch06 config 预告「喜欢×擅长×重要三者交集」——重要之事（`values`）实际在 ch07 step-2 才登场，口径略偏；建议决策：改 ch06 预告文案为「喜欢×擅长组合、工作目的（含价值观）筛选」，或保留并在 ch07 进入提示中说明 `values` 在 step-2 登场。

---

## 3. 设计侧修复清单（本轮已落地）

| # | 项 | 文件 | 说明 |
|---|---|---|---|
| 1 | 跨章注入 step_context | `chapter_config_ch07.json` | `step_context.references=[talents,likes,work_purpose,values]`（P0-A） |
| 2 | 去孤儿键 importance | config / PRD / 落地 | 改 `values`（P0-B） |
| 3 | 两栏→三栏 | `ch07_exercise_mockup.html` | 对齐 ch05/ch06 |
| 4 | 提交按钮 + 提交态 | `ch07_exercise_mockup.html` | 提交本步 + 409/锁定 |
| 5 | 分桶标签 / 角色术语 / 导出含未定 | `ch07_exercise_mockup.html` | P2 修复 |
| 6 | §7.7 翻新 | `工程实施交接文档.md` | 2 步 / `ideal_works` / 4-ref / 补 `step_context` 行（P1-A） |
| 7 | 落地 §4/§5/§9 内部矛盾 | `ch07_落地级步骤设计.md` | 统一「step_context+mentor_hooks 双注入」（消除与 §8.3 冲突） |

## 4. 开发侧待办（契约已定义，按落地 §10.2 落地）

compaction.py 在三处白名单加 `ideal_works` + reseed 消 DRIFT / assembler ch07 组合·分桶合并块 / steps.py ch07 前置 gate（校验 ch4/5/6 已提交）+ 输出校验分支 / `renderCh07StepBody` 嵌套渲染器（`ideal_works` 含 like_source/strength_source 只读 + bucket 下拉 + 锁定）/ 契约测试（级联 + 409）/ 草稿真实持久化+失败提示 / 整体导出 UI / ch8 消费契约确认（`mentor_hooks.references=[ideal_works]`）。
