# ch05 双专家评审记录（2026-08-10）

> 评审方式：架构评审 + 产品/UX 评审两个**只读** agent 并行，基于当前 ch05 四份交付物（config / PRD / 落地设计 / mockup）重跑。
> 整改归属原则（依 2026-08-09 权责界定）：**设计侧** = config / PRD / 落地文档 / mockup（页面逻辑与数据库设计）；**开发侧** = 运行时代码（assembler / steps / agent / compaction）、数据库 reseed。开发侧事项设计侧只在文档指出、请开发处理，不动代码。

## P0（阻断项）

### P0-1 架构｜DB active 配置仍是旧「九宫格」单步版，新 config 运行时零生效 —— 开发侧待办
- **问题**：`chapter_configs` 表 ch05 active=1/version=1/1163B，仅 1 个 exercise、`step_role_id=career_counselor`、**无 `step_context`/`ui_context` 键**。连带放大：`assembler.py:508-509` `step_context=cfg.get("step_context")` 得 None → 回退 `mentor_hooks.references=["work_purpose"]`，落地 §9.6「显式 `[]` 阻断 mentor_hooks 泄漏」在运行时**不成立**，work_purpose 会进 step prompt。
- **整改**：reseed 后跑 `check_config_consistency.py --chapter ch05` 消除 DRIFT。

### P0-2 架构｜现有 ch05 测试硬编码旧单步，新 config 上线必红 —— 开发侧待办
- **问题**：`test_m4_per_chapter.py:51-55`（role/payload 仅 `talents`）+ `_run_one_chapter:194/200/205`：role 断言失败、`declared`（新增 `commentary`/`user_manual`）不满足、单步 submit 断言 `compacted is True` 而 2 步章 `_maybe_compact`（`steps.py:446`）需全步提交；`test_all_chapters_e2e.py:21` 同源错误。
- **整改**：修 `test_m4_per_chapter`/`test_all_chapters_e2e` 的 ch05 期望（psychologist、3 字段、2 步）；新增 `test_m4_ch05_cascade.py`（对齐 ch04），覆盖 step-1→step-2 `talents` 级联注入、`preserve_output` 锁定保留、`commentary`/`user_manual` 不进 profile 三条契约。

### P0-1 UX｜Step 1 提交后映照点评被自动跳转隐藏 —— 设计侧已修
- 原 `submitStep1()` 写完 `#step1out` 末行即 `goStep(2)` 切走面板，用户永远看不到刚生成的映照点评。与 PRD §6.3「Step 2 卡片微动展开提示」相悖。已改为**原地渲染 + Step 2 导航项脉冲提示**，不再自动跳转。

### P0-2 UX｜Step 1 缺「去回答 30 问 / 查 100 例」入口 —— 设计侧已修 + 开发侧待办
- Step 1 计数器低于 10 只变琥珀色，下方按钮区无任何补充入口；全文件检索无「30 问」，30 问抽屉完全未实现；100 例入口原仅存在于 Step 2。直接违反 PRD §7 两条验收与落地 §7，且是 ch04 P0-2「入口只在单侧」同类重犯。已加 Step 1「查 100 例 / 去回答 30 问」入口（warn 态高亮）；30 问清单由题库服务接入（开发侧）。

### P0-3 UX｜提交门槛与 config 契约冲突 —— 设计侧已修
- 原 `if(countFilled()<1)` 答 1 题即可提交；但 config 5 问均 `required:true`，PRD §2.2 明确「5 问全部完成后可提交」。原型会误导前端放行，后端按 required 校验将直接 422。已改门槛 = **5 题全答**，缺项红框定位。

## P1（应修项）

### P1-1 架构｜ch07 gating 不认 ch05 talents —— 开发侧待办
- `context_requirements.py:11-14` 把 `talents` 生产者登记为 ch03；PRD §1.6 / 落地 §5 声称「ch7 必需 ch05 talents、缺失整章锁定」，代码不会拦截。开发侧补 ch07 前置判定读 ch05 `status`/`talents`。

### P1-2 架构｜profile 预算可能静默吃掉对象列表 —— 开发侧待办（低优先级）
- `compaction.py:41` 800tok≈3200 字符；`talents` 升级为 `[{text,rating,locked}]`（20 项≈900+ 字符）叠加 values/ranked/likes/importance/intersection 后 `_trim_to_budget:95-97` 兜底 `next(iter(result))` 可能弹出 MANDATORY 字段。注：`talents` 本身已在 `MANDATORY`（ch03 预留），风险可控；建议对落库前剥离 `locked` 或提高预算。

### P1-3 架构｜mockup 与运行时提交契约不符 —— 设计侧已修（部分）
- 提交门槛已对齐 config（P0-3）；`reference_strengths` 已在 mockup 建模为**抽屉瞬时输入**（编辑态独立展示，不与 `talents` 混排），点「引用」即加入编辑中 `talents` 并标 `source:'100_examples'`，**提交时与 step-1 长处合并去重，一并落库**（非独立持久字段；详见设计侧回复 §3.2 / 落地 §10 P1-7）。

### P1-4 UX｜regen 未保持锁定项原位置 —— 设计侧已修
- 原实现把锁定项一律置顶（`[...locked,...unlocked]`），与后端 `merge_preserved_output:434-450` 按**原位置**保留语义不一致，且 ≤2 未锁定时有概率空反馈。已改为**锁定项占原索引、未锁定项就地洗牌**，toast 写明保留数；无项可排时提示「全部已锁定」。

### P1-5 UX｜文案黑话回潮（ch04 P1-8 重犯）—— 设计侧已修
- 「教练映照点评（commentary）」「初步长处（talents，10+ 目标）」改为业务口径「教练的映照」「初步长处清单（先攒到 10 个）」。

### P1-6 UX｜《使用说明书》无导出 —— 设计侧已修
- 卡片右上加「导出」按钮（下载 `.md`）。

### P1-7 UX｜`reference_strengths` 未被建模 —— 设计侧已修 + 开发侧实现
- 原 `addFromExample` 直接 push 进 `talents`，全文件无该字段痕迹，开发照原型会漏掉 step-2 的 `user_answers` 契约。现引用项进入独立 `referenceStrengths` 数组并标记「引自100例」、计入 N；开发侧实现时 step-2 `user_answers.reference_strengths` 单独提交（不混入 `talents`）。

### P1-8 UX｜缺「每问单独保存草稿」—— 设计侧已修
- 每张问卡内置「保存此题草稿」按钮 + 时间戳；保留全局「保存全部草稿」。

## P2（非阻塞，记录备查）
- 落地 §8.5 仍把 `preserve_output` 透传列为待办，**实际已实现**（`steps.py:88/317/353`、`agent.py:200/241`、`assembler.py:402/421/556`）；ch04 P1-6 亦已修（`values`/`ranked` 已入 `MANDATORY:39`）。设计侧已更新落地 §8.5 口径。
- mockup `regen()` 锁定项语义现已与后端 `merge_preserved_output`（原位置）一致。
- PRD §4 示意缺 `instruction`、`commentary` 缺 `readonly`；文档写「`step_context = []`」而 config 为 `{references:[]}`（靠 `(step_context or {})` 兜住，仅措辞差异）。
- schema:105 声明 `item_template`，运行时读 `item_schema`（`steps.py:611`）—— 全章通病，非 ch05 独有。
- 演示长处仅 5 个，无法演示「达 10 个后计数转正常态」；100 例抽屉已扩到 70 条真实书例（演示充足）。
- Step 1 提交前计数现为「已作答 X / 5 问」，提交后转「已收集 N / 10 个长处」（P2 已修）。
- 发现分叉副本 `platform/prototype/ch05_exercise_mockup.html` 与 `llm_prompt_design/platform/prototype/` 并存；已统一为**仓库根 `platform/prototype/` 单一权威版**（删除 `llm_prompt_design` 副本，旧根副本已备份至 `.backups_ch05_20260810/`）。

## 测试实跑
`check_config_consistency.py --chapter ch05` **当前必报 DRIFT**（`drift=core_concepts,exercises,few_shot_examples,guiding_questions,output_template`；`missing_in_db=step_context,ui_context`）——因 DB 仍为旧九宫格（P0-1）。建议顺序：reseed → 重跑一致性脚本 → 修 `test_m4_per_chapter`/`test_all_chapters_e2e` 的 ch05 期望 → 新增 `test_m4_ch05_cascade.py`。现有基线 26 passed 不含 ch05 新契约，resed 后需同步更新测试。

## 开发侧待办清单（请开发处理）
1. **reseed** ch05 config（覆盖旧九宫格），消除 consistency DRIFT（P0-1）。
2. **assembler.py**：确认 `instruction` 注入（已吸收 ch04 P0-1）；`mentor_hooks.references` 每步注入用法厘清（P0-1 运行时泄漏修复，让 `step_context:[]` 真正生效）。
3. **tests**：`test_m4_per_chapter.py` / `test_all_chapters_e2e.py` 改 ch05 期望（psychologist / 3 字段 / 2 步）；新增 `test_m4_ch05_cascade.py`。
4. **context_requirements.py**：ch07 gating 读 ch05 `talents`（P1-1）。
5. **compaction.py**：`talents` 落库前剥离 `locked` 可选（P1-2，低优先级）。
6. **题库服务**：接入 30 问 / 100 例（P0-2 UX 开发部分）。
7. **前端 `renderStepOutput`**：补带 ◎〇△ 评级的 `talents` 渲染分支 + 100 例抽屉 + 使用说明书卡（PRD §8 最高优先级）。
