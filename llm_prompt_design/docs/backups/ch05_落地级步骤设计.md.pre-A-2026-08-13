# 第五章 落地级步骤设计（ch05 擅长的事 / 才能）

> 📌 **开发侧阅读入口（唯一交接文件）**：本文是「ch05 擅长的事（才能）」的权威设计契约，讲清**每一步用户做什么、产出什么、约束与 LLM 角色、跨步依赖**。开发交接**只需本文件 + 本文件路由到的文件**，不另发整包。
> **本文与权威文件的分工（各管一层，本文为意图/索引层）**：
> - **字段契约 / 运行时参数**（exercises、`input_schema` / `output_fields` 类型与 `lockable`/`readonly`、跨步 `references.from_exercise`、`mentor_hooks`/`references`）→ `config/chapters/chapter_config_ch05.json`（**权威源**：运行时读的是 `chapter_configs` 表中由本 JSON 灌入的 active 行，并非直接读本文件。改完本 JSON 后必须发布到 DB（seed/save）并跑 `scripts/check_config_consistency.py` 确认无漂移，否则会出现"文档已改、运行仍跑旧配置"的漂移）
> - **本章 LLM 角色系统提示原文**（psychologist / mentor）→ `docs/PRD/PRD-ch5.md` §3.1
> - **视觉与交互对齐**（2 步导航、五问卡、◎〇△ 评级列表、使用说明书卡、100 例抽屉）→ `platform/prototype/ch05_exercise_mockup.html`（可点击原型）；像素级见 Ardot 设计稿（fileId 待建，当前以 mockup 为交互参考）
> - **原书第五章正文** → `chapter_md/第五章-只要找到_擅长的事_，就可以应用到工作中.md`
> ⚠️ **冲突裁决**：若本文（尤其 §8 对 config 的描述）与实际 `chapter_config_ch05.json` 不一致，**以 config JSON 为准**——本文为解释性说明，可能因后续微调而滞后。

## 0. 本章在全书旅程里的位置
原书第五章（只要找到擅长的事，就可以应用到工作中）是全书「擅长」维度的唯一深化章。全书结构：

```
ch1 误区 → ch2 内外标准 → ch3 三要素公式（框架/种子）
   → ch4 重要(价值观) → ch5 擅长(才能) → ch6 喜欢 → ch7 组合 → ch8 实践
```

- ch05 自身 = **2 个真实 step 级联**（step-1 回答 5 问找长处 → step-2 评级◎〇△ + 写《使用说明书》），把「擅长的事」从模糊习惯澄清为可沉淀的跨章种子 `talents`。
- **上游依赖**：练习本身**无硬性上游**（step_context = []，step 不读任何前章档案），可独立进入。但 `mentor_hooks.references=["work_purpose"]` 引用 ch4 的 `work_purpose`，让刘老师开场回响「你第四章定的工作目的…，现在第五章找你擅长的事」，建立 ch4→ch5 连续性。
- **与 ch03 的关系（务必注意）**：ch03 已产出**初始** `talents` 键（三要素框架的「擅长」初版）。ch05 是「擅长」的**权威深化章**，用同名 `talents` 键覆盖写入带 ◎〇△ 评级的正式版本——ch05 是 `talents` 的权威写方，ch3 初版作为种子被深化。
- **下游产出**：
  - `talents`（带 ◎〇△ 评级）写入 `learner_profile`，供后续章引用；
  - **ch7（组合）** 读 `talents`（以◎级为主）与 `likes` / `work_purpose` 结合，得出「真正想做的事」（必需）。
- **与 ch04 的结构差异（务必注意）**：ch04 是「5 个独立 exercise（step）」级联；ch05 是「**2 个 step**」（step-1 5 问 → step-2 评级+说明书），靠 config 的 `references.from_exercise` 级联。运行时由 `build_step_prompt` 从同章已 submitted 的 `step_runs.parsed_output` 取前序字段注入（详见 §2 / §5）。

## 1. 一句话目标
让用户通过 2 步，把无意识的「擅长的事」澄清为：10+ 长处清单（带 ◎〇△ 评级）→ 一份《使用说明书》概述，并沉淀为跨章种子 `talents`（以◎级为主供 ch7 组合），供 ch7 组合、ch8 实践引用。

## 2. 「一步还是多步」先说清（最关键）
- **ch05 = 2 个真实 step（exercise），不是 1 步多阶段，也不是 5 步**。对开发侧说"ch5 有 2 步"是准确的；这与 ch04 的"5 步"、ch03 的"1 步 4 阶段"都不同。
- 2 个 step 通过 config 的 **`references.from_exercise`** 级联（schema 明确为"解决 ch4 五步级联"设计，ch05 复用同一机制）：
  - step-2 注入 step-1 的 `talents`；
  - step-2 另读用户输入的 `reference_strengths`（100 例清单引用）。
- 每步是独立 step 容器、独立提交态、独立锁定；runtime 从同章已 submitted 的 `step_runs` 取前序字段注入（受 ≤3k assembler 护栏约束，仅按需注入、不全量）。
- `mentor_hooks.references=["work_purpose"]` 仅供导师对话，绝不进入 step prompt（与 ch03/ch04 同构的 `step_context` 解耦）。

## 3. 步骤流程图
```mermaid
flowchart TD
    S1[step-1 回答5问找长处] --> S2[step-2 评级+使用说明书]
    S2 --> Done[本章完成]
    M[导师对话 刘老师 贯穿全程] -.引用 ch4 work_purpose.-> S1
    S2 -.talents(◎级优先).-> C7[ch7 组合原料]
```

## 4. 逐阶段明细
> 每个 step 的「谁 / 输入 / 输出 / 记录 / 约束 / LLM 角色 / 依赖」如下。

### step-1 回答 5 个问题，找出擅长的事
- 谁：用户 + LLM（psychologist，辅助模式 assist）。
- 输入：用户在 5 个长文本框逐题作答（书中精选 5 问：充实体验 / 最近烦躁的事 / 问身边人 / 辞职后留恋 / 取得的成果与做法）。5 问为固定书本题（配置即生效，见 config `input_schema`），用户只填答案。
- 输出：`commentary`（整体映照点评，markdown，readonly）+ `talents`（10+ 初步长处，editable_list，lockable）。
- 记录：答案存 `user_answers`；LLM 输出存 `parsed_output`。**流程（平台状态机）**：五问填写 → 点击「运行 LLM（/run）」生成初步长处（saved 草稿）→ 审阅映照点评 → 点击「提交（/submit）」锁定本步（与 step-2 同一状态机，`/submit` 不调 LLM）。
- 约束：LLM **不替用户定性**，只映照反复出现的才能线索；`talents` 用户可编辑/锁定。
- 依赖：**无上游**。mentor 引用 ch4 `work_purpose` 仅作开场回响，非练习输入。
- 想不出时：引导「书写冥想（3 分钟不停笔）」或「提问对话」，并渲染 config 的 `guiding_questions` 与 100 例清单（见 §7）。

### step-2 评级与使用说明书
- 谁：LLM（psychologist，assist）+ 用户审阅。
- **数据流时序（严格遵守平台状态机：运行 LLM(/run) → 编辑/评级/锁定(saved) → 提交(/submit)）**：
  1. step-1 **运行 LLM（/run）** → 产出 `step-1.talents`（仅 5 问答案衍生，saved 草稿，**此时不含任何 100 例引用项**）；用户审阅后**提交（/submit）** 锁定该步（方可供下游引用、归档 profile）。
  2. step-2 进入 → 用户在「擅长的事 100 例清单」抽屉点「引用」 → 该项**仅写入 `user_answers.reference_strengths`**（用户输入态；不并入 `step-1.talents`、不进入 `talents` 编辑列表）；
  3. 用户点「**运行 LLM（/run）**」 → 后端读取已 submitted 的 `step-1.talents` 与本次 `reference_strengths`，**合并去重**后送入 LLM → 产出 `parsed_output.talents`（canonical 候选清单，saved 草稿，含 `source:'100_examples'` 标记）；
  4. 用户在 saved 草稿上完成 ◎〇△ 评级、增删、锁定 → 点「**提交（/submit）**」确认本步（仅此时可供下游引用、归档 profile）；
  5. 后续 step-2 重生成（/run 新建草稿）→ `preserve_output.talents` 锁定项原位置保留；非锁定项可重排/重生成。
- 输入：注入 step-1 已 submitted 的 `talents` + 本次 `user_answers.reference_strengths`（来自 100 例抽屉引用，list_of_items，可选）。
- 输出：`talents`（带 ◎〇△ 评级的 10+ 长处，editable_list，lockable）+ `user_manual`（《使用说明书》概述，markdown，readonly）。
- canonical schema：**step-1 `{text, locked, source?}`（无 rating）**；step-2 `{text, rating, locked, source?}`；`source` 仅 100 例引用项为 `'100_examples'`，5 问衍生为 `null`。
- **100 例引用合并与去重规则（开发侧实现约束）**：
  - 用户在抽屉点「引用」**仅写入 `reference_strengths`**；点击「运行 LLM」时后端把 `step-1.talents` ∪ `reference_strengths` 合并；
  - 去重：对 `text` 做首尾空白清理 + 连续空白归一化后**精确去重**（不做语义近似合并）；文本相同的项**保留 step-1 衍生项**（`source:null`），100 例项不覆盖；
  - 用户手动新增项 `source` 固定为 `null`；仅来自抽屉「引用」的项可标记 `'100_examples'`；
  - `reference_strengths` **不作为 profile 字段或独立数据库列持久化**；它仅随当前 step-2 run 的 `user_answers` 保存，用于可追溯的本次 LLM 输入（草稿可重新进入，不会因不进 profile 而丢失）。
- 记录：`parsed_output`；`talents` 沉淀到 `learner_profile`（覆盖 ch3 初始 `talents`）。
- 约束：用户给每项标 ◎〇△（◎有充实感与成功有关 / 〇有充实感 / △不确定），**允许留空（不强制）**；可增删、锁定；LLM 不替用户定性、只建议评级；重生成时锁定项保留（preserve_output）。
- 依赖：references step-1 `talents`。向后：→ ch7（talents，以◎为主）。

### 贯穿：导师对话（刘老师）
- 谁：用户 + 刘老师（mentor）。
- 输入：用户自由提问；刘老师上下文 **仅读取已 `submitted` 的 step_runs 输出**（ch05 的 step-1/step-2 已提交结果）+ **ch4 的 `work_purpose`**（mentor_hooks.references 钩子锚点，仅作开场回响）。
- 输出：苏格拉底式追问，帮用户从 5 问挖出无意识才能、把「所以」重写成「正因为」、陪写使用说明书。
- 约束：严格遵守全局硬规则——**导师只读 submitted 输出，绝不读草稿或未提交 LLM 结果**；`work_purpose` 仅作导师钩子，绝不进入 step-1/step-2 的普通分析 Prompt（那两处的上下文由 `step_context`/`ui_context` 显式声明，当前均为 `[]`）；不替用户定性。LLM 角色：导师（刘老师），贯穿，不限步骤。

## 5. 依赖总览
- step-1：无上游依赖。
- step-2 → 注入 step-1 `talents`（均为同章前序 step 输出，runtime 取已 submitted 的 step_runs）。
- 贯穿导师对话：依赖 **ch4 `work_purpose`**（跨章锚点，非练习输入）。
- step-2 产出：`talents` → 供 **ch7** 引用（必需，以◎级为主组合原料）；ch3 初始 `talents` 被本章覆盖。

## 6. 各产出字段的锁定类型（lockable / readonly 契约）
本节说明 ch05 **每个产出字段在"提交前"是否用户可锁定 / 只读**，是给开发侧的字段级声明。⚠️ "锁定"只管本章内，管不了提交后其他章节的改写——后者见本节点末尾表格。

- **talents**（step-1）：`editable_list, lockable=true` —— 用户可锁初版长处，锁后 LLM 不再改。
- **talents**（step-2）：`editable_list, lockable=true` —— 用户可锁带评级的长处，重生成保留；**以 step-2 的 `talents`（带 ◎〇△）为最终落库版本**。
- **commentary**（step-1）：`readonly`（LLM 产出，用户不可改）。
- **user_manual**（step-2）：`readonly`（LLM 产出概述，用户不可改）。

### 跨章改写硬约束（必须和上表分开理解）
ch05 的持久产出（`talents`）写入 `learner_profiles.profile_json` 后，**不被后续任何章节覆盖**：

| 字段 | ch05 内可锁定？ | 后续是否被改写 | 改写方 | 能否保值到 ch07 |
|---|---|---|---|---|
| talents | 可（step-1/step-2） | **不会被改写** | 无后续章写 `talents` | **是（ch07 以◎级为主读取）** |

> ch06（喜欢）只写各自的深化键 `likes`，不碰 talents；ch7 读 talents（只读引用）、work_purpose、likes 做组合，不覆盖写入。故 ch05 的 `talents` 是稳定的跨章种子。
> 注意：ch03 已写过初始 `talents`，ch05 在本章用同名键覆盖为带评级的正式版——这是预期的「深化覆盖」，ch05 是 `talents` 的权威写方。

## 7. 用户可见文案与前端提示
> 以下文案为设计侧提供的 UI 建议，开发侧渲染即可，不在前端写死业务逻辑；标签/引导属前端展示层。

- **2 步导航**：左栏 step-nav 显示 step-1～step-2，标注每步标题与提交状态（未开始/草稿/已提交）；点击切换中心内容区。
- **step-1 引导小字**：「这 5 个问题没有标准答案。答不上来时，试试『书写冥想』：定时器 3 分钟，不停笔写『我擅长的事』；或找朋友做『提问对话』。」
- **step-1 计数刷新**：顶部显示「已收集 N / 10 个长处」，低于 10 高亮「去回答 30 问 / 查 100 例」；长处增删时**实时刷新计数**（吸取 ch03 mockup「计数不刷新」教训）。
- **step-2 ◎〇△ 评级**：每项长处左侧三级选择器——◎（赭石高亮，有充实感与成功有关）/ 〇（墨色，有充实感）/ △（浅灰，不确定）；顶部高亮「◎ 级是你第七章组合『想做的事』的主原料」。
- **step-2 使用说明书卡**：底部纸张卷轴样式显示 `user_manual` 概述，可导出。
- **100 例清单抽屉**：同 ch4 风格，可搜索、可引用长处——点「引用」**仅写入 step-2 的 `reference_strengths`（编辑态用户输入，不并入 `talents` 列表）**；点击「运行 LLM」时后端将其与 step-1 的 `talents` 合并去重，生成候选 `talents` 并标记 `source:'100_examples'`。
- **空态/兜底**：各步必要输入缺失时拦截并提示；LLM 明显偏差时提供「换一种说法 / 重新生成」按钮（新建草稿 run，不影响已提交记录）。
- **配色口径**：ch05 继承 ch4 书卷气体系（赭石 accent、纸纹、衬线字体），见 PRD §6，mockup 已按此渲染。

## 8. 数据表与字段设计
> 已对照实 backend 代码核对（compaction.py / db.py / steps.py / schema）。**ch5 不新建表、不新增列，全部复用现有结构。**

### 8.1 复用的两张现有表
- **`learner_profiles`**（`user_id` PK, `profile_json` TEXT）：跨章档案。ch5 持久产出 `talents` 作为新键落进 `profile_json`。
- **`step_runs`**（`id` PK, `user_answers`, `parsed_output`, `status` …）：各 step 原始输入存 `user_answers`；LLM 输出存 `parsed_output`。现有列已够。

### 8.2 profile_json 里新增的键（白名单已含，零改动）
`compaction.py` 的 `PROFILE_FIELDS` **已包含 `talents`**（且 `talents` 同时出现在 `STRUCTURED_FIELDS` 与 `MANDATORY_DOWNSTREAM_FIELDS`，来自 ch03 预留——ch3 四键 likes/talents/importance/intersection 已在白名单）。ch5 提交后 `compact_profile` 自动并入，**无需改白名单、无需改 compaction 逻辑**。
- `talents`：`editable_list`，`lockable=true`；落库形态为 `[{text, rating, locked}]`（step-2 版本覆盖 ch3 初始字符串列表）。
- `commentary` / `user_manual`：**不进 profile_json**（不在白名单），仅留在各 step 的 `step_runs.parsed_output`，属章内中间产物/只读概述，不跨章传播。

### 8.3 ch5 练习配置（现状，已对齐 new-schema）
`chapter_config_ch05.json` 当前状态（**开发侧无需改，仅渲染**）：
- 2 个 exercises（step-1 / step-2），各自 `step_role_id`（psychologist）、`output_fields` 类型与 `lockable`/`readonly` 如上 §6；
- step-1 `input_schema` = 5×`text`（书中 5 问，配置即生效）；
- step-2 `references.from_exercise` 级联 step-1 `talents`；
- `mentor_hooks.references: ["work_purpose"]`（引用 ch4 锚点）；
- `step_context: {references: []}`、`ui_context: {references: [], strength_examples: [...]}`（与 ch03/ch04 同构；`strength_examples` 为 ch05 专属的「擅长的事 100 例清单」数据源，随 active config 发布，前端直接读取已加载的 chapter config 即可，不另接接口）。

### 8.4 锁定机制（复用整步提交态）
**真实的「锁定」= 整步提交态**（已核实 `routers/steps.py`，与 ch02/03/04 同构）：
- 用户编辑草稿：`edit_output` 在 `status=="submitted"` 时返回 409；只有 `saved` 态可改。
- 用户「锁定」：`submit_step` 把 `status` 置为 `submitted`，并触发 compaction 归档。
- 重跑保护：`_get_or_create_run_id` 在 latest=submitted 时新建 run id（不覆盖已提交记录）。
**结论**：各字段「锁定」= 用户编辑草稿后点「提交/确认并保存」，整步进入 submitted 即锁。`lockable`/`readonly` 仅作 schema 意图声明（前端据此渲染「可锁/只读」UI），运行时强制靠提交态。

### 8.5 数据落地结论
- 不新建数据库表/列、无表结构迁移；但需完成：配置发布（reseed active config）、运行时上下文（`step_context` 声明 / `preserve_output` 透传给 steps/agent/assembler）、前端 renderer（可编辑带 ◎〇△ 的 `talents`、锁定、重生成）与契约测试。属「无 schema 迁移，但有代码/前端/配置改动」。
- 唯一持久键 `talents` 借 `learner_profiles.profile_json` 落库，提交即自动归档（白名单已含）；中间产物 `commentary`/`user_manual` 仅留 `step_runs`；
- 唯一变动是常规分章配置 `chapter_config_ch05.json`（已就绪）；
- 锁定靠整步提交态；与 ch02/03/04 同构：2 个 exercise 级联。

### 8.6 草稿持久化契约（V4-4，可验收）
> 草稿 = 尚未「运行 LLM（/run）」或已 saved 但未「提交（/submit）」的用户输入；**绝不进入 submitted 输出、下游 step、导师正式上下文或 `learner_profile`**。以下为用户可见规则，开发侧据此实现并加回归测试。

- **step-1 逐题草稿**：每题卡内置「保存此题草稿」按钮 + 显示「上次保存于 ……」（时间戳）；并提供全局「保存全部草稿」。草稿（5 问答案）须**跨页面刷新与重新进入本步完整恢复**，不得仅存浏览器临时态。
- **step-2 引用草稿**：用户在「100 例清单」抽屉点「引用」写入的 `reference_strengths`（/run 前）**同样必须持久化并回显**——用户选择抽屉项后离开/刷新再回来，引用项不丢失。
- **草稿与 submitted 隔离**：草稿仅承载于 `user_answers` / 既有的草稿态 step_run，与 `/run` 后的 `saved` 结果、`/submit` 的 `submitted` 正式结果明确隔离；草稿不可被下游 `references.from_exercise`、导师 `mentor_hooks` 或 compaction 读取。
- **保存失败处理**：保存（单题/全量）失败必须给出**明确错误提示**，严禁显示「已保存」但实际丢失；建议失败重试 + 状态回滚到保存前。
- **回归测试要求**：覆盖「保存 → 刷新/离开 → 回到本步 → 输入（含 100 例引用项）完整恢复」。

## 9. 设计判断（与旧稿 / git HEAD 的差异处理）
本轮按 ch02/ch03/ch04 新范式重做 ch05，对旧稿做了以下关键纠偏，特此说明，避免开发侧混淆：
1. **权威源更正**：git HEAD 中旧 `chapter_config_ch05.json` 是单步「九宫格擅长梳理」（8 角度 worksheet、career_counselor、不同 core_concepts），已作废。已更正为 **`chapter_config_ch05.json`（new-schema，2 步）** 为唯一权威源。
2. **2 步 = step-1 5 问 + step-2 评级说明书**：与书本「回答 5 问找擅长 → 归纳使用说明书」一致，且复用 `references.from_exercise` 级联（与 ch04 同机制）。
3. **step-1 输入配置即生效**：5 问建模为 `input_schema` 的 5×`text`（label=题面），不再依赖前端硬编码问句。
4. **`talents` 数据模型升级**：step-2 产出 `{text, rating, locked}` 对象列表（带 ◎〇△），覆盖 ch3 初始字符串列表；下游 ch7 以 `rating=="◎"` 优先。
5. **`mentor_hooks.references` 由 git HEAD 旧配置（无/错）改为 `["work_purpose"]`**：对齐 mentor_design_spec 八章钩子表 ch05 行，支撑刘老师"第四章工作目的"开场回响。
6. **`step_context` / `ui_context` 显式解耦**：均设为 `[]`，阻断 assembler 回退 `mentor_hooks` 泄漏进 step prompt 的 bug（同 ch03/ch04 已修路径）。

## 10. 开放点与专家评审
> 以下为设计侧已识别、待评审/开发侧确认的点；专家评审结论将于评审后回填本节点。

- **100 例清单数据源（已定稿，V4-3）**：「擅长的事 100 例清单」的**权威载体 = ch05 active config 的 `ui_context.strength_examples`**（随 config 发布，前端读取已加载 chapter config，不另接接口、不硬编码常量、不复用 ch04 的 `value-examples` 接口）。每项结构 `{id, text, becomes_strength?, becomes_weakness?}`（id 稳定如 s01..s100，仅作 UI key/去重，不传 LLM）；已写入完整 100 条（见 `chapter_config_ch05.json`）。「30 问清单」仍待题库服务接入（开发侧），不在 config 内。**（设计已确认：用户从 100 例清单点「引用」仅写 step-2 `reference_strengths`（对象数组 `[{text}]`），点击「运行 LLM」时后端合并去重、标 `source:'100_examples'`；「100 例清单」抽屉为前端呈现细节。）**
- **`talents` 数据模型迁移**：ch3 初始 `talents` 为字符串列表，ch5 step-2 升级为 `{text, rating, locked}` 对象列表；以 step-2 为准覆盖。**下游 ch7 读取 ch05 `talents`，`rating=='◎'` 优先组合原料（设计侧已确认；属开发侧 5.5 待办落实 `context_requirements.py`）**。
- **◎〇△ 交互**：采用**选择器**（◎/〇/△ 三按钮，明确优于拖拽）；锁定项必须按原位置保留（`preserve_output` 强制要求）。
- **prerequisite gating**：ch7 进入时读取 ch5 的 `status` 与 `talents`，缺失则按 PRD-ch5 §1.6 锁定——属下游章职责，本文仅声明产出，不实现 gating。
- **step-2 空评级下游语义（已拍板·方案 2：允许留空，不强制）**：提交时**允许保留未评级项**（`rating==""`）；UI 文案为「未评级 / 待定」；profile 形态与已评级项一致（`rating:""`）；**ch07 组合时仅以 `rating=="◎"` 为组合原料**，未评级项不参与组合原料、但作为候选信息保留（不进入 ◎ 优先集合）。

### 专家评审结论（回填区）

> 双专家评审（架构 + 产品/UX）已于 2026-08-10 重跑，完整记录见 `docs/ch05_专家评审记录.md`。以下为结论与整改归属摘要（与 ch04 流程一致）。

- **设计侧已修（本轮）**：
  - P0-1 UX：Step 1 提交后映照点评改为**原地渲染 + Step 2 导航脉冲提示**，不再自动跳转隐藏（原与 PRD §6.3 相悖）。
  - P0-2 UX：Step 1 计数器低于 10 时补「查 100 例 / 去回答 30 问」入口（warn 态高亮）；30 问清单由题库服务接入（开发侧）。
  - P0-3 UX：提交门槛对齐 config，改为 **5 问全答**方可提交，缺项红框定位（原答 1 题即放行，后端按 required 将 422）。
  - P1-3：mockup 提交契约对齐 config（门槛=5 问）；`reference_strengths` 建模为**抽屉瞬时输入**（编辑态独立展示），点「引用」**仅写入 `reference_strengths`**，点击「运行 LLM」时后端合并去重、标 `source:'100_examples'`（**非独立持久字段**）。
  - P1-4 UX：regen 锁定项改**按原位置保留**（非置顶），toast 写明保留数；无项可排提示「全部已锁定」。
  - P1-5 UX：清理文案黑话（"commentary"→"教练的映照"、"talents（10+ 目标）"→"初步长处清单（先攒到 10 个）"）。
  - P1-6 UX：《使用说明书》卡加「导出」按钮（下载 .md）。
  - P1-7 UX：`reference_strengths` **抽屉瞬时输入**（编辑态独立展示，不并入 `talents`）；点「引用」**仅写入 `reference_strengths`**，点击「运行 LLM」时后端合并 `step-1.talents` ∪ `reference_strengths` 去重，生成候选 `talents` 并标 `source:'100_examples'`（`reference_strengths` 仅随 step-2 run 的 `user_answers` 保存，不进 profile / 独立列）。
  - P1-8 UX：每问卡内置「保存此题草稿」+ 时间戳；保留全局「保存全部草稿」。
  - P2：分叉副本统一为**仓库根 `platform/prototype/` 单一权威版**（删除 `llm_prompt_design/` 副本，旧根副本备至 `.backups_ch05_20260810/`）；落地 §8.5 口径更新（**后端链路已支持 `preserve_output`**；ch05 **前端透传**判定（新版 `output_schema.type=structured`，非 `editable_list`）**与 `talents` 专用 renderer（带◎〇△/锁定/regen 原位置保留）尚未完成**，属开发侧 5.2/5.3 待办）。
- **开发侧待办（请开发处理，设计侧不动代码）**：
  - P0-1：**reseed** ch05 config 覆盖旧九宫格，消除 `check_config_consistency.py --chapter ch05` DRIFT（`missing_in_db=step_context,ui_context`）。
  - P0-2：`test_m4_per_chapter.py` / `test_all_chapters_e2e.py` 改 ch05 期望（psychologist / 3 字段 / 2 步）；新增 `test_m4_ch05_cascade.py`（对齐 ch04，覆盖 step-1→step-2 `talents` 级联、`preserve_output` 锁定保留、`commentary`/`user_manual` 不进 profile）。
  - P1-1：`context_requirements.py` 补 ch07 gating 读 ch05 `status`/`talents`（现登记为 ch03 生产者，缺失不拦截）。
  - P1-2：`compaction.py` 落库前剥离 `talents.locked`（低优先级，`talents` 已在 MANDATORY，风险可控）。
  - 题库服务：接入 30 问 / 100 例（P0-2 UX 开发部分）。
  - 前端 `renderStepOutput`：补带 ◎〇△ 评级的 `talents` 渲染分支 + 100 例抽屉 + 使用说明书卡（PRD §8 最高优先级）。
- **测试**：`check_config_consistency.py --chapter ch05` 当前**必报 DRIFT**（DB 为旧九宫格 P0-1）；建议顺序 reseed → 重跑一致性 → 改测试期望 → 新增级联测试。基线 26 passed 不含 ch05 新契约，reseed 后需同步更新。

### 深度复核结论（回填区 · 2026-08-11 第二轮，双专家）

> 在 V4 四项修复（V4-1~V4-4）落盘后，应需求拉**架构设计专家 + 产品管理专家**做第二轮深度 review。完整记录见 `docs/ch05_专家评审记录_v2.md`。结论：**架构与设计双层面均无 P0，设计合理、不会再有问题**；唯一产品侧 P1（step-2 评级图例缺失）已于本轮在 mockup 修复。

- **架构专家**：P0=0；config↔schema 一致（`strength_examples` 已同步 schema，`reference_strengths` 对象数组 `[{text}]` 一致），100 例数据流自洽，状态机同构（submitted 态 409），`talents` 零迁移；残留 P1=4 全为**开发侧实现跟踪项**（后端合并去重未结构化、ch07 gating 缺 ch05 判定、`talents` 落库未剥 `locked`、契约测试缺口），P2=2（schema `item_template` 命名偏差、评审记录历史注记）。
- **产品专家**：P0=0；三栏布局/100 例抽屉/◎〇△ 控件/空评级/文案一致性均 OK；唯一 P1（评级图例）**设计侧已修**（step-2 列表上方加 ◎〇△ 含义图例，文案复用 PRD §1.4）；P2=若干（epolar 极性配色、toast 文案对齐均已修；localStorage 演示/step-2 引用草稿时间戳/整体导出/九宫格深挖引导/个人应用笔记为开发侧或后续迭代建议）。
- **设计侧本轮已修（仅 mockup 原型，未动运行时代码）**：① step-2 评级图例；② 「运行 LLM 汇总」→「让教练汇总并建议 ◎〇△」文案对齐（左栏步骤说明/reflabel/引用 toast 三处）；③ 100 例「用对/用错」绿橙配色。备份见 `.backups_ch05_20260811_v7/`。
- **开发侧待办（请开发处理，设计侧不动代码）**：reseed 消 DRIFT / 后端合并去重结构化 / ch07 gating / `talents` 剥 `locked` / ch05 契约测试 / 草稿真实持久化+失败提示 / 整体导出 / 30 问题库接入。详列见 `docs/ch05_专家评审记录_v2.md` 第三节。
