# 重构计划 v2.0 — 设计侧 Review 改动清单

> 用途：设计侧在 review 开发计划文档时直接修正的事实性错误/补全，请开发 Agent 同步更新到自己手上的文档记忆中。
> 生成时间：2026-08-03

## 改动明细（共 6 处）

1. 端口澄清（§7 Q1，原文约 L209–212）
   - 改动：把"待拍板问句"改为"✅ 已澄清（按 8011 推进）"。
   - 依据：实测 config.py 后端 BACKEND_PORT=8011，之前记忆的 3011 来自 FRONTEND_ORIGIN 的陈旧默认值。
   - 后续：M5 顺手把 FRONTEND_ORIGIN 那个陈旧变量也改掉。

2. 配置转换闭环（§7 Q2，原文约 L214–217）
   - 改动：把"待拍板问句"改为"✅ 已闭环（无需再问）"。
   - 依据：8 章 config 的 output_fields 已全部类型化（T-A 已完成）。M2 阶段只做校验，不再转换。

3. M3 伪代码角色加载（原文约 L91–92）
   - 改动：原代码 `role = load_role(exercise['llm_role'])` 概念错误，改为：
     `step_role = load_role(cfg['step_role_id'])`
     `mode = exercise['llm_role']`
   - 注释：llm_role（assist/guide/none）是辅助模式，不是角色名，不要当成角色去 load。

4. M3 前序输出注入护栏（原文约 L95，user= 那行）
   - 改动：补注释 "prior 由 build_step_prompt 的 ≤3k 护栏裁剪，勿全量注入前序输出"。

5. SPEC 对齐说明（§3 SPEC 行，原文约 L151）
   - 改动：补 "（⚠️ 已于 2026-08-03 对齐重构；原版已备份；无需再改 SPEC）"。

6. 修订说明（§0，原文约 L20）
   - 改动：补 "（其中 Q1 端口、Q2 配置转换已澄清/闭环；Q3、Q4 仍待确认）"。

## 设计侧认同但未改的两处（请开发 Agent 知悉）

- Q3：step_runs 不加 user_id 列（同意原建议方案 A）。
- Q4：ch04 step-5 不读 ch3 profile（同意原建议方案 A）。
- 以上两条设计侧倾向同意原建议，但属于待开发 Agent 最终确认项，故未改动。

## 协作边界提醒

- 开发计划（plan）文档与后续 progress 文档归开发侧过程管理。
- 设计侧只 review 提修改意见，不直接改这两个文件。
- 本清单是设计侧替开发 Agent 梳理的"我改了什么"，方便转发与同步。
