# ch05 专家评审记录（v2 · 2026-08-11 双专家深度复核）

> 本文为设计侧溯源文档（不进落地文档顶部 refer，仅供设计侧追溯）。
> 背景：V4 复审 4 项（V4-1~V4-4）已全部修复并落盘；用户要求拉**架构设计专家 + 产品管理专家**在理解系统架构与需求后，再做一轮深入 review，确认设计合理、不会再有问题。
> 评审方式：两个只读子 Agent 并行，分别读取 `config/`、`schema/`、`docs/ch05_落地级步骤设计.md`、`docs/PRD/PRD-ch5.md`、`platform/prototype/ch05_exercise_mockup.html`、原书 ch05 md、运行时 `platform/backend/app/`（compaction/assembler/step_runner/chat）。

---

## 一、架构设计专家结论

**总判断**：V4 四项修复已在 config/文档落地，设计层**无 P0 矛盾，可交付开发**；残留项均为开发侧已跟踪实现项或 P2 健康度，不阻塞架构。

1. **config ↔ schema 一致性 — OK（1 处 P2）**
   - `ui_context.strength_examples`：config 每项 `{id,text,becomes_strength?,becomes_weakness?}` 与 schema `ui_context.properties.strength_examples.items.required=["id","text"]` 完全匹配；V4-3 已正确同步 schema。✓
   - `reference_strengths`：config `input_schema` 用 `item_schema:{fields:[{name:text}]}`，与 PRD/落地「对象数组 `[{text}]`」一致。✓
   - **P2**：schema `input_schema` 声明的是 `item_template`（行 105），而 config 实际用 `item_schema`（ch03/ch04 同此偏差，运行时 `steps.py` 读 `item_schema`）。属全章通病，运行时正确、schema 滞后，违反「字段须与 schema 一致」但非 ch05 独有 → 建议统一命名（跨章，非 ch05 必修）。

2. **100 例数据流 — OK（1 处 P1，开发侧）**
   - `reference_strengths` 正确定义为 step-2 `input_schema` 字段（落 `user_answers`），不是 `output_fields`，未被误并入 `talents`；`references.from_exercise` 仅级联 step-1 `talents`，二者解耦自洽。✓
   - **P1**：文档称「点击运行 LLM 时**后端**合并去重、标 `source:'100_examples'`」，但 runtime（assembler/prompt/step_runner）当前无合并代码，`reference_strengths` 仅随 `user_answers` 进入 prompt、由 LLM 指令去重——确定性去重与 source 标记未由 config+runtime 结构性保证。建议 assembler 加 ch05 专用 merge/dedup 分支（对齐 ch04 step-2 特判）。**属开发侧已跟踪项。**

3. **状态机与锁定 — OK**
   - 落地 §8.4「运行→提交（`/submit` 不调 LLM）；submitted 态 `edit_output` 返 409」与 `steps.py:385`「submitted step output is locked」同构。✓
   - 草稿靠 `user_answers`/`step_runs` 持久化，与 submitted 隔离，无冲突表述。✓

4. **下游消费与 DRIFT — OK（2 处 P1，均开发侧）**
   - `talents` 已在 `PROFILE_FIELDS`/`STRUCTURED_FIELDS`/`MANDATORY_DOWNSTREAM_FIELDS`（compaction.py 17/34/39），step-2 带 `rating` 的对象数组被原样落库，下游 ch07 读 `rating=="◎"` 兼容；**零新增表/列/迁移**。✓
   - **P1-1**：`context_requirements.py` 仍把 `talents` 生产者登记为 ch03，ch07 gating 缺失 ch05 判定。**开发侧待办。**
   - **P1-2**：`talents` 落库前未剥离 `locked`（低优先级，已在 MANDATORY，风险可控）。**开发侧待办。**
   - **P0-1（开发侧）**：DB 仍为旧九宫格，须 reseed 消除 consistency DRIFT（ch04 先例）。**开发侧待办。**

5. **契约测试覆盖 — P1（缺口，开发侧）**
   - `test_assembler.py` 仅泛化引用 `talents`；`test_step_runner.py` 无 ch05/`reference_strengths`/`strength_examples`/级联/409 用例。缺 `strength_examples` 载体、`reference_strengths` 合并、状态机 409 场景。**评审记录 P0-2 已列，开发侧待补。**

6. **旧数据流残留 — OK（1 处 P2）**
   - 权威 `ch05_落地级步骤设计.md` 全文为「点引用仅写 `reference_strengths`；运行 LLM 时后端合并」正确口径（行 63-64/71/117/189/193）；config label（行 114）已清理「即加入 talents/提交时并入」。旧词仅存于 `_待设计确认*` 历史文档与评审记录（行 34 为「已修」历史注记）。
   - **P2**：建议评审记录该处加删除线避免误读。

**架构汇总**：P0=0；P1=4（后端合并未结构化实现、ch07 gating、talents 剥离 locked、测试缺口，均开发侧）；P2=2（schema `item_template` 命名、评审记录历史注记）。**架构层面无遗留矛盾，可交付开发。**

---

## 二、产品管理专家结论

**总判断**：无 P0，普通用户可顺畅走通旅程；需修 1 个 P1（评级图例）及若干 P2。**产品/UX 层面无遗留致命问题，可交付。**

1. **三栏布局与原书对齐 — OK**：中央展示原书定义/叙事（非 5 问居中）；step-1 五问、step-2 评级均在右栏「步骤」tab。✓
2. **100 例抽屉 — OK + 2×P2**：
   - 极性有文字（`用对：becomes_strength｜用错：becomes_weakness`），但 `.epolar` 原无专属样式、未视觉强调。**→ 已修（本次深度复核轮，加绿/橙配色）**。
   - step-1 抽屉点「引用」后 toast 写「点击运行 LLM 汇总时合并」，但 step-2 按钮名是「让教练汇总并建议 ◎〇△」，文案错位。**→ 已修（本次，全量统一为真实按钮名）**。
3. **◎〇△ 评级 — 控件 OK，缺图例 P1（已修）**：
   - 三按钮直观；空评级提交与落地 §10「允许留空」一致（`submitStep2` 不拦截，标「未评级」）。
   - **P1（已修）**：step-2 界面原无评级图例，用户看不到「◎有充实感且与成功有关 / 〇有充实感 / △目前还不确定」。**→ 已在 step-2 评级列表上方加图例行（文案复用 PRD §1.4 / 原书）。**
4. **草稿保存 UX — 意图 OK，原型未演示持久化 P2（开发侧）**：
   - step-1「保存此题草稿 + 已存 HH:MM」、提交态视觉区分符合 §8.6。
   - **P2**：① 原型为静态，未用 localStorage 演示刷新恢复；② step-2 引用草稿无时间戳/保存指示；③ 无保存失败提示路径。需开发侧按 §8.6 落地真实持久化与失败提示。
5. **导出/交付物 — 部分 OK，P2**：`user_manual` 有「导出」(.md) 已定义（PRD §6.2/§7）。**P2**：才能清单+◎〇△ 评级本身无整体导出入口，建议补整体导出（书要求「最少 10 个」清单）。
6. **文案一致性 — 整体 OK（图例 P1 已修）**：「教练的映照」「初步长处清单（先攒到 10 个）」等已修黑话；按钮/标签与 PRD、落地对齐。
7. **需求覆盖度 — 核心 OK，P2**：
   - 覆盖书 5 问、◎〇△、使用说明书、100 例、ch7 联动。
   - **P2**：书「问题 5 的 8 角度九宫格深挖（表 5-1）」未做独立交互练习，仅由 LLM 映照/导师/30 问承载 → 建议以提示或折叠区引导。
   - **P2**：使用说明书为 LLM 只读生成，用户未「写」应用方法 → 可加每才能的个人应用笔记字段。

**产品汇总**：P0=0；P1=1（评级图例，已修）；P2=若干（epolar 样式[已修]、toast 文案[已修]、localStorage 演示、step-2 引用草稿时间戳、整体导出、九宫格深挖引导、个人应用笔记）。

---

## 三、整改动作与归属（深度复核轮）

### 设计侧已修（本轮，仅 mockup 原型，未动运行时代码）
- **P1 评级图例**：step-2 评级列表上方加 ◎〇△ 含义图例（含「ch7 以◎为主」「目标 10/理想 20」），文案复用 PRD §1.4。
- **P2 toast/reflabel 文案对齐**：「运行 LLM 汇总」→「让教练汇总并建议 ◎〇△」（左栏步骤说明、reflabel、引用 toast 三处统一为真实按钮名）。
- **P2 极性视觉**：100 例抽屉「用对/用错」加绿/橙配色（`.epolar .good/.bad`）。
- 备份：`.backups_ch05_20260811_v7/ch05_exercise_mockup.html.orig`。

### 开发侧待办（请开发处理，设计侧不动代码）
- **P0-1 reseed**：ch05 config 覆盖旧九宫格，消除 `check_config_consistency.py --chapter ch05` DRIFT（`missing_in_db=step_context,ui_context`）。
- **P1 后端合并去重**：assembler 加 ch05 专用 merge/dedup 分支，结构性保证 `step-1.talents ∪ reference_strengths` 去重并标 `source:'100_examples'`（对齐 ch04 step-2 特判）。
- **P1 ch07 gating**：`context_requirements.py` 补 ch07 读 ch05 `status`/`talents`（现登记为 ch03 生产者）。
- **P1 talents 剥离 locked**：`compaction.py` 落库前剥离 `talents.locked`。
- **P1 契约测试**：`test_assembler.py`/`test_step_runner.py` 补 ch05 `strength_examples`/`reference_strengths`/级联/409 场景。
- **P2 草稿真实持久化 + 失败提示**：按落地 §8.6 落地 localStorage/后端持久化与保存失败明示（原型仅静态演示意图）。
- **P2 整体导出**：才能清单+◎〇△ 评级整体导出入口。
- **P2 题库服务**：30 问清单接入（100 例已由 config 载体解决）。

### 设计侧开放建议（P2，非阻塞，可后续迭代）
- 书问题 5 的 8 角度九宫格深挖：以提示/折叠区引导（不另做重交互）。
- 使用说明书：可加每才能「个人应用笔记」用户输入字段。

---

## 四、最终结论

- **架构层面**：无 P0 矛盾，config/schema/落地三处契约一致，`talents` 零迁移，状态机同构，可交付开发。
- **产品/UX 层面**：无 P0，用户旅程可顺畅走通；唯一 P1（评级图例）已于本轮修复。
- **剩余项**：全部为开发侧实现跟踪项（reseed / 后端合并 / ch07 gating / locked 剥离 / 测试 / 真实持久化 / 整体导出），设计侧已给出清晰契约与字段定义，开发侧按契约落地即可，不会产生新的设计歧义。
- **结论**：设计合理，不会再有问题；待开发侧按上述待办收口后，ch05 即可端到端贯通。
