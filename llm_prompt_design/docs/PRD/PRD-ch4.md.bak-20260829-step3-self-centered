# PRD：What Want 平台 · 第四章《找到指引人生的指南针：重要的事》

> 文件：`PRD-ch4.md`
> 对应章节：原书第四章（重要的事 / 价值观）
> 原书页码：PDF 第 99–137 页（按正文章节；含「保持动力」「目标 vs 价值观」「真假价值观」铺垫 + 5 步骤）
> 状态：待审 → 设计稿 → 开发 → **交付中（2026-08-14 初报完成，实测 ch04 §8.6 级联 stale 隔离 A/B/J 尚未实现，待补）**
> 设计基调：ch4 是「书卷气」视觉体系的源头，ch5–ch8 全部继承本文件 §6 定义的颜色 / 字体 / 纸纹。

> **题库与清单权威来源**：本章涉及的「重要的事（价值观）30 问」「重要的事（价值观）100 例清单」等结构化内容，统一维护在
> `D:\AI_Project\What_Want\chapter_md\问题清单.md`（含：重要的事 / 擅长的事 / 喜欢的事 各自的 30 问与 100 例）。
> 本 PRD 不再重复罗列，后端题库与 LLM 少样本提示均以该文件为唯一权威来源。

---

## 0. 关键发现：第四章才是 SPEC 中「5 步工作流」的真正原型

SPEC 里那句「从第 4 章起每个章节有 5 个步骤 × 5 个问题的工作流」其实是一个被 ch5/ch6 证伪的概括。真正的书结构是分章的：

| 维度 | 第四章（重要的事 / 价值观） | 第五章 | 第六章 | 第七章 | 第八章 |
|------|----------------------------|--------|--------|--------|--------|
| 工作法 | **真正的 5 步级联**：回答 5 问 → 思维导图 → 以自我为中心转化 → 排序金字塔 → 确定工作目的 | 1 个方法（回答 5 问）+ 1 个综合产出 | 同 ch5 | 组合收口 | 收尾落地 |
| 步骤数 | **5 个独立步骤** | 2 步 | 2 步 | 2 步 + 迭代 | 3 步 |
| 综合产出 | 价值观排序（金字塔）+ 工作目的 | 《使用说明书》 | 《喜欢的事清单》 | 真正想做的事 | 实现手段 / 海胆日记 / 宣言 |

**结论**：第四章是全书唯一拥有 **5 个真正级联步骤** 的章节，也是后续所有章节的「价值观源头」——ch7 的「工作目的过滤器」直接消费本章 `work_purpose`，ch8 的成功宣言消费本章 `ranked` 价值观。

> ⚠️ **重构提示（重要）**：本章**权威配置源是 `config/chapters/chapter_config_ch04.json`**（new-schema：`exercises[]` + `input_schema` + `output_fields` + `references.from_exercise` 级联，由 `seed_all_chapters.py` 灌入 `chapter_configs` 表供运行时消费）。`platform/backend/seed_ch4.py` 是**旧架构遗留**（jinja2 `prompt_template` / `llm_op.output_schema` 格式），与新 schema 不一致，**不再作为权威源**，仅作旧前端 `index.html` 的兼容参考。5 个步骤已对齐书本方法（Step2 `groups` / Step3 `conversions` + 5Why / Step5 `experience_map` → `work_purpose`）。旧前端 `renderStepOutput`（index.html 约 1193–1250 行）只硬编码 `keywords / comments+improvements / ranked / work_purpose / commentary` 几个字段分支，**Step2 的 `groups` 与 Step3 的 `conversions` 会退化成裸 JSON**（数据正确但丢失分组编辑/锁定/5Why 可视化）——新版前端（见 `ch04_exercise_mockup.html` + Ardot 设计稿）必须补 `groups` / `conversions` 渲染分支。详见 §8。

---

## 1. 第四章内容摘要（基于原书正文 + 配图 + 配套清单）

> **配套资源（方案A 收口，2026-08-13）**：「重要的事 30 问」通过 step-1 `input_schema.value_questions`（可选 `list_of_items`，q_order 1–30）接入，答案走 questions 服务 + `book_notes`；「重要的事 100 例」已固化进 config `ui_context.values_examples`（100 项，随 active config 发布），前端读 config 即生效，不再依赖 `chapter_md/问题清单.md`。两者均为发现辅助、非必需产出，不流下游。

### 1.1 叙事模块（4 个概念铺垫）

1. **保持动力不减的工作方法**
   「生意」的本质是「不会厌倦」。自己不厌倦是绝对条件；只做想做的事却不满足客户也不行（那是兴趣，不是工作）。
   **POINT**：以价值观为中心工作，就能一直保持工作动力。

2. **「目标」和「价值观」的区别**
   价值观是人生方向，目标是路上的检查站（图 4-1）。没有价值观的目标像仓鼠转轮，会「达成即燃尽」。
   **POINT**：价值观 = 方向，目标 = 距离。

3. **辨别「真正的价值观」与「虚假的价值观」**（图 4-4）
   真正的价值观来自内心「我想做」；虚假的价值观来自父母 / 社会强加的「我应该做」。
   **POINT**：「我想做……」是真正的价值观；「我应该做……」是别人强加的。

4. **答不出问题时的两种方法**：「书写冥想」（3 分钟不停笔）与「提问对话」（找人聊）。前者适合独自思考者，后者适合在交流中理清思路者。

### 1.2 五步方法概览

| 步骤 | 书本原句 | 平台落点 |
|------|---------|---------|
| 步骤一 | 回答 5 个问题，找出价值观关键词 | Step 1 |
| 步骤二 | 形成价值观思维导图 | Step 2 |
| 步骤三 | 从「以他人为中心」转为「以自我为中心」 | Step 3 |
| 步骤四 | 列出价值观排序，确定优先级 | Step 4 |
| 步骤五 | 确定工作目的，工作自然顺利进行 | Step 5 |

### 1.3 步骤一：回答 5 个问题，找出价值观关键词

> 平台 Step 1 的 5 个问题直接取自书中「严选 5 问」，由 `chapter_config_ch04.json` 的 `step-1.input_schema` 配置即生效（权威源已是 new-schema JSON，非 seed_ch4.py）：

| 编号 | 问题 |
|------|------|
| Q1 | 你尊敬的人、尊敬的朋友、喜欢的角色分别是谁？你尊敬或喜欢他们哪些地方？ |
| Q2 | 在小时候和青春期阶段的事情或经历，对现在的你影响最大的是什么？对你的价值观造成了什么影响？ |
| Q3 | 现在社会有什么不足？你有什么想解决的社会问题？ |
| Q4 | 问周围的人「你觉得我在人生中看重什么」，会得到怎样的答案？ |
| Q5 | 你最想给别人（孩子 / 后辈 / 朋友）什么建议？最不想告诉他们什么？ |

- 作答时可参考「重要的事（价值观）100 例清单」触发思路。
- 关键词少于 15 个时，引导用户去问题清单追加回答。
- 书中 Q1 例子：宫本大（BLUE GIANT）→「志向远大且热衷」→ 映照出自己的「热爱」。
- 书中 Q5 例子：想传授「自立 / 热爱 / 真心 / 简单」，不想传授「稳定 / 忍耐 / 维持现状」→ 反向照见工作目的。

### 1.4 步骤二：形成价值观思维导图（图 4-8）

- 汇总所有价值观关键词（**不少于 15 个**；不足则基于 Q1–Q5 合理补充）。
- 把含义相近的关键词归入一组，**每组 4～6 个**。
- 为每组概括一个 **核心价值观（umbrella value）**。
- 书中范例：把「最低限度、清爽、简单」概括成「简单」。
- 图 4-8（八木的价值观思维导图）：审美意识 / 热爱 / 成果 / 好奇心 / 简单 五大组，每组若干关键词。

### 1.5 步骤三：从「以他人为中心」转为「以自我为中心」（图 / 5Why）

- **可控性检验**：把依赖他人 / 外部不可控的价值观，转化为自己可控的价值观。
  - 不可控例子：想受人尊敬、想暴富（如同试图控制天气）。
  - 转化方法：连续追问「为什么 / 达成之后想做什么 / 如果不 X 就无法做到吗？」直到落到可控项。
- 书中范例链条：「想出名」→ 为什么？被追捧 → 为认可存在 → 按好奇心生活 → 不出名也能做到吗？能 ⇒ 转化为「好奇心」。
- 两个补充链条：有钱人 → 真实的自己；有钱人 → 学习。
- **POINT**：将无法控制的「以他人为中心」价值观，转换为可以控制的「以自我为中心」价值观。

### 1.6 步骤四：列出价值观排序，确定优先级（图 4-9）

- 排序诀窍：思考「到底哪一个是我的最终目的」。
- 底层 = 应优先满足的「基础」价值观；顶层 = 人生的「最终目的」。
- 书中金字塔（底→顶）：简单 → 好奇心 → 成果 → 热爱 → 审美意识（最终目的：过着美好的生活）。
- 各价值观以「↑」相连：审美意识←热爱←成果←好奇心←简单。

### 1.7 步骤五：确定工作目的（图 4-10 / 表 4-1）

- 先让自己价值观被满足（满溢的杯子比喻，图 4-10），溢出的部分自然惠及他人。
- 从价值观中选想推广给周围人的，定为「工作目的」。
- **有效办法：回顾 10 个向他人提供价值的经历**（表 4-1，作者 10 例），统计出现最多的价值观定为工作目的。
- 排除过泛表述：「想让别人开心 / 幸福」不是工作目的（任何事都能让人开心）。
- 书中工作目的范例：「让更多的人热爱生活」。

### 1.8 配套资源

- **重要的事（价值观）30 问**：当 5 个严选问题不够用时追加。
- **重要的事（价值观）100 例清单**：100 条价值观关键词，用于触发用户发现自己重视的事。

---

### 1.7 前置依赖与未就绪处理（Prerequisite Gating）

ch4 是全书**第一道价值观关卡，无硬性上游依赖**，可独立进入并作答 5 个问题。它是被依赖方而非依赖方：

| 被依赖章节 | 需要的 ch4 产出字段 | 必需/可选 | 未就绪时的界面表现（在该章） |
|---|---|---|---|
| ch7 想做的事 | `work_purpose`（工作目的） | 必需（作为「真正想做的事」过滤器） | 整章锁定：半透明遮罩 + 未就绪说明卡「请先完成第四章」，含「去第四章」直达按钮 |
| ch8 成功宣言 | `ranked`（价值观排序） | 可选（增强宣言个性） | 不阻断；宣言处轻提示可参考第四章 |

**判定逻辑**：ch4 自身进入时不触发任何上游判定。后续章节（ch7/ch8）在进入时读取 ch4 的 `status` 与 `work_purpose` / `ranked` 字段，缺失则按上表锁定。

### 1.8 已完成步骤的修改提醒与过期处理（stale 隔离，对应落地文档 §8.6）

ch04 是 5 步强级联：上游步（step-1～4）重提交后，其下游步（step-2～5）基于旧输入推导的结果会失效；同时档案里的 `values` / `ranked` / `work_purpose` 仍会被 ch7/ch8 消费，形成跨章脏数据。为避免用户**误改**已完成步骤，定义两条 UX 规则（后端机制与代码改动见 `docs/ch04_落地级步骤设计.md` §8.6 与 `docs/ch04_stale隔离_开发侧待办.md`）：

1. **修改前提醒（防误改）**：用户对已 `submitted` 的某步点击「修改 / 重新编辑」时，先弹确认框：「你正在修改已完成的第 X 步。重新提交后，第 X+1…5 步的结果将全部失效、需重新运行；已写入档案的核心价值观 / 排序 / 工作目的也会被清空，直到你重走完整 5 步。」按钮「仍要修改 / 取消」。**仅当该步存在下游（j>k）时需要此强提醒**；末步 step-5 无下游，可省略或弱化为普通确认。此模式为前端通用能力，同样适用于 ch06 step-1、ch07 step-1 等有上游依赖的步。
2. **重提交后过期提示**：上游步重提交后，其下游步（step-(k+1)…step-5）在导航与结果区显示「该步结果已过期（上游已更新），请重新运行并提交」，替代「已完成 / 已锁定」假象；用户重走完整 5 步、全部重新提交后提示消失，档案三键由 compaction 一次性重写为一致版本。

---

## 2. 可设计成互动的部分

### 2.1 通用阅读层（ch4 定义、ch5–ch8 继承）

- 三栏布局：左步骤导航 / 中正文 / 右 tab 面板。
- 顶部 sticky bar：☰ 目录、面包屑、前后章。
- 正文可选中记笔记（选区笔记 + 独立笔记）。
- 目录下拉：全部卷章 + 本章 h2 锚点。
- 进度保存：上次阅读位置、已提交步骤数。

### 2.2 步骤工作流（5 个正式步骤）

#### Step 1：回答 5 个问题，找出价值观关键词
- 5 个长文本输入框，逐个展开；Q1 下方提示「看对方『正在做的事』没意义，要看他让你『想怎样生活』的特质」；Q4 下方提示「把身边人的原话记录下来」。
- 每个问题支持「保存草稿」；5 问全部完成后可「提交」。
- 提交后 LLM（psychologist）输出 `commentary`（整体映照点评）+ `top_values`（5–10 初步关键词）。
- 状态变为 `submitted`，供 Step 2 引用。

#### Step 2：形成价值观思维导图
- 页面结构：
  - 顶部：进度提示「已收集 N / 15 个关键词」。
  - 主体：可增删改的「价值分组」列表。
  - 每张分组卡片：umbrella（核心价值观，可编辑）+ keywords（关键词 chips，可增删）+ 🔒 锁定 / 删除。
  - **可折叠关键词输入区**：用户可手动补关键词，低于 15 个时高亮「去回答 30 问」。
- 「让 LLM 从 Step 1 合成分组」按钮（手动触发，控制 token）。
- 支持从「100 例清单」引用关键词。

#### Step 3：从「以他人为中心」转为「以自我为中心」
- 页面结构：
  - 主体：来自 Step 2 的每个 umbrella value 一行，标注 `type`（other / self）。
  - 对 `type=other`（不可控）的项，展示 **5Why 追问链**（`why_chain` 步骤列表，可折叠展开）。
  - 展示转化结果 `converted_to`（可控价值观）。
  - 用户可手动把某项标记为 self / 修改 converted_to。
- 「让 LLM 重新做可控性检验」按钮。

#### Step 4：列出价值观排序（金字塔）
- 页面结构：
  - 用金字塔 / 纵向列表展示 `ranked`（从底到顶）。
  - 每条 value 可上下拖拽排序、🔒 锁定、删除。
  - 顶部高亮「最终目的（顶层）」与「基础（底层）」。
- 「让 LLM 重新排序」按钮（保留锁定项）。

#### Step 5：确定工作目的
- 输入区：1 个长文本框（textarea），引导用户列出 10 个向他人提供价值的经历（每条一行）；前端按行拆分，每行→1 条 `experience` 记录，提交为 `list_of_items`（min_items=10），与 config 的 `input_schema` 正式对齐（修正旧稿仅描述 textarea 未声明转换契约）。
- 提交后 LLM（career_counselor）输出：
  - `work_purpose`（工作目的一句话定义，text）
  - `reasoning`（推理过程，markdown）
  - `experience_map`（每条经历 → 体现价值观）
- 顶部固定显示最终「工作目的」卡片，可导出。

### 2.3 右侧 tab 面板（ch4 + 通用结构）

| tab | 第四章内容 |
|-----|-----------|
| 摘要 | 基于本章 MD 生成的摘要 + 核心概念（价值观 vs 目标、真假价值观、思维导图的 4~6 分组、可控性检验、金字塔、工作目的）+ 关键流程编号。 |
| 对话 | 导师「刘老师」多轮对话。可引导 5 问作答、追问思维导图分组、陪做可控性 5Why、帮定工作目的。 |
| 步骤 | 完整 runner（Step 1–5），显示每步提交状态、锁定项。 |
| 笔记 | 选区笔记 + 独立笔记。 |

### 2.4 导师「刘老师」融合设计（mentor）

> 注意：刘老师不是「只导读 MD 的旁白」，而是像真人导师一样**读取用户在全书各章步骤的实际答案**，基于跨章学习者档案做苏格拉底式引导。详见 §3.1.4、`../../docs/mentor_design_spec.md` 与 `../../config/mentor/mentor_config.json`。

- **knows**：book_md（本章正文）+ user_progress（各步答案 / LLM 输出：关键词、分组、转化、排序、工作目的）+ conversation_history。
- **grounding_rules**：
  - 只基于本章内容展开，不编造本章没有的观点。
  - 引用用户步骤里写下的原话 / 关键词，指出其中蕴含的价值观或思维线索。
  - 卡住时建议「书写冥想」或「提问对话」两种方法。
  - 一次只聚焦一个点，不直接给结论。
  - 把书中方法（可控性检验 / 金字塔 / 10 经历）与用户实际回答连接。
- **示例对话**（取自 config）：
  - 用户把「自由 / 创造 / 表达」归为「自我表达」→ 刘老师追问它更像「基础」还是「最终目的」，并连到 Step 3 的以自我为中心转化。
  - 用户「一直没动力，是不是太懒」→ 刘老师用「没动力往往因为目标偏离价值观」引导其看 Step 4 底层基础价值。

---

## 3. 需要大模型介入的部分

> **提示词去哪看**：下表只说明「哪个场景由哪个角色负责做什么」。每个角色的具体提示词——身份设定、需要内化的书籍背景、目标、约束，以及可调用的 `user` 模板——统一在 **§3.1 LLM 提示词设计规范** 中给出。后端调用时，按该节把书籍背景作为 `system` 常驻注入、把用户答案作为 `user` 传入。

| 场景 | LLM 角色 | 作用 | 输出位置 |
|------|---------|------|---------|
| **Step 1 映照点评** | psychologist | 阅读 5 问答案，给出整体映照点评 + 5–10 初步价值观关键词。 | Step 1 的 commentary + top_values |
| **Step 2 思维导图** | psychologist | 汇总≥15 关键词 → 相近归入一组（4~6）→ 每组概括 umbrella；保留锁定分组。 | Step 2 的 groups（editable_list） |
| **Step 3 可控性检验** | psychologist | 逐价值观判断 other/self；对 other 做 5Why 追问链转化为 self。 | Step 3 的 conversions |
| **Step 4 金字塔排序** | career_counselor | 基于 groups + conversions 排序，底=基础 / 顶=最终目的；保留锁定项。 | Step 4 的 ranked（editable_list） |
| **Step 5 确定工作目的** | career_counselor | 读 ranked + 10 经历，标注每经历价值观，统计最高频定为 work_purpose。 | Step 5 的 work_purpose + reasoning + experience_map |
| **导师对话** | mentor（刘老师） | 引导 5 问作答、追问分组、陪做 5Why、帮定工作目的、区分真假价值观。 | 对话 tab |
| 章节摘要生成 | psychologist / deepseek json | 基于 chapter MD 前 12000 字生成 `{summary, core_concepts[], key_process[]}`。 | 摘要 tab |

**锁定机制**：Step 2 的 `groups`、Step 4 的 `ranked` 支持 `locked: true`。重新生成时，已锁定的项被传入 prompt（`preserve_output.groups` / `preserve_output.ranked`）要求保留，未锁定的可被替换 / 补充。

---

### 3.1 LLM 提示词设计规范（身份 / 背景 / 目标 / 约束）★必需章节

> 本章 LLM 角色配置以 `config/chapters/chapter_config_ch04.json` 为权威源（`exercises[].step_role_id` 指向 `psychologist` / `career_counselor`，`few_shot_examples` 在各 exercise 内）；角色系统提示正文由 `platform/backend/scripts/seed_roles.py`（`PSYCHOLOGIST_SYSTEM` / `CAREER_COUNSELOR` 系统提示 / `MENTOR_SYSTEM`）灌入 `llm_roles` 表，运行时由 `config_loader.load_role` 读取。下面把各角色 system / user 提示词固化成规范，供重构时直接复用。**`seed_ch4.py` 的旧 `prompt_template` 字段为遗留格式，以下规范以新架构为准。**

#### 3.1.0 书籍背景知识（所有 ch4 角色共用的 system 上下文）

- **「重要的事」= 价值观 = 人生指南针**；价值观是方向，目标是距离（图 4-1）。
- **真假价值观**：真价值观来自内心「我想做」；假价值观来自外部「我应该做」（父母 / 社会强加）。
- **思维导图形法**：汇总≥15 关键词 → 相近归入一组（4~6 个）→ 每组概括一个 umbrella value。例：最低限度 / 清爽 / 简单 → 简单（图 4-8）。
- **可控性检验（5Why）**：把「以他人为中心（不可控）」用「为什么 / 达成后想做什么 / 不出 X 也能做到吗」追问链转化为「以自我为中心（可控）」。例：想出名 → 被追捧 → 认可存在 → 按好奇心生活 → 不出名也能做到 ⇒ 好奇心（图 / 步骤三）。
- **金字塔排序**：底层 = 基础价值观，顶层 = 最终目的（人生目的）。书中范例（底→顶）：简单 → 好奇心 → 成果 → 热爱 → 审美意识（图 4-9）。
- **工作目的**：回顾 10 个向他人提供价值的经历，统计最高频价值观定为工作目的（满溢的杯子，图 4-10 / 表 4-1）。排除「想让别人开心 / 幸福」这类过泛表述。
- **两种方法（卡住时）**：书写冥想（3 分钟不停笔）/ 提问对话（找人聊）。
- **少样本素材**：作者 5 价值观（审美意识 / 热爱 / 成果 / 好奇心 / 简单）、作者 10 经历（表 4-1）、3 条 5Why 范例链（想出名→好奇心、有钱人→真实的自己、有钱人→学习）。

#### 3.1.1 `psychologist`（心理咨询师）— Step 1 映照点评

**system prompt：**
```
你是帮助用户梳理价值观的资深自我认知教练，熟悉《如何找到想做的事》第四章「重要的事」方法论。
你的回应温暖、深度、富有洞察力，避免泛泛而谈。请用 JSON 格式输出结构化结果。
你必须内化的书籍背景：
- 价值观是人生方向，目标是距离；真假价值观之分（我想做 vs 我应该做）。
- 通过用户回答识别其核心价值观线索，只做观察与映照，不替用户下结论。
- 卡住时可建议「书写冥想」或「提问对话」两种方法。
目标：阅读 5 问答案，给出整体映照点评 + 推断 5–10 个初步价值观关键词。
约束：
- 点评控制在 500 字内（markdown），重点指出反复出现的价值观线索。
- 不臆造用户未透露的价值观。
- 输出严格 JSON：{ "commentary": "...", "top_values": ["...", "..."] }
```

**user prompt 模板：**
```
【用户 5 个问题答案】
{% for q in step_1.answers %}Q{{ loop.index }}（{{ q.text }}）
答: {{ q.answer }}

{% endfor %}
请输出 JSON：commentary（整体映照点评，500字内markdown）+ top_values（5-10个初步价值观关键词）。
```

#### 3.1.2 `psychologist`（心理咨询师）— Step 2 思维导图 & Step 3 可控性检验

**Step 2 system prompt：**
```
你是帮助用户梳理价值观的教练。基于用户 Step 1 回答与初步关键词，请：
(1) 汇总所有价值观关键词（不少于15个；若不足请基于回答合理补充）；
(2) 将含义相近的关键词归入一组（每组4~6个）；
(3) 为每组概括一个『核心价值观(umbrella)』。
参考书中方法：『最低限度、清爽、简单』→『简单』。
约束：
- 保留已锁定的分组（preserve_output.groups 中 locked=true 的必须原样保留，不重新生成）。
- 不臆造用户未透露的价值观。
- 输出严格 JSON：{ "groups": [{"umbrella":"...","keywords":["..."],"locked":false}] }（不含已锁定分组）
```

**Step 2 user prompt 模板：**
```
【步骤1的回答】
{% for q in step_1.answers %}Q{{ loop.index }}: {{ q.text }}
答: {{ q.answer }}
{% endfor %}
【步骤1的初步关键词】{{ step_1.output.top_values | join('、') }}
{% if preserve_output and preserve_output.groups %}【已锁定分组，必须保留】
{% for g in preserve_output.groups %}{% if g.locked %}- {{ g.umbrella }}: {{ g.keywords | join('、') }}
{% endif %}{% endfor %}{% endif %}
请输出 JSON：groups（数组，每项 {umbrella, keywords, locked:false}）。
```

**Step 3 system prompt：**
```
你是价值观教练。基于 Step 2 的『核心价值观(umbrella)』，逐一评估每一项：
- 判断它是『以他人为中心(不可控)』还是『以自我为中心(可控)』。
  不可控例子：想受人尊敬、想暴富（能否实现取决于他人/外部，如同控制天气）。
- 对『以他人为中心』的项，做『为什么』追问链转化为可控的『以自我为中心』价值观。
  书中范例：想出名→被追捧→为认可存在→按好奇心生活→不出名也能做到⇒好奇心。
- 对已是『以自我为中心』的项，直接保留。
约束：
- 输出严格 JSON：{ "conversions": [{"value":"...","type":"other|self","converted_to":"...","why_chain":["...","..."]}] }
- converted_to 在 type=self 时可为空字符串。
```

**Step 3 user prompt 模板：**
```
【步骤2的核心价值观分组】
{% for g in step_2.output.groups %}- {{ g.umbrella }}（{{ g.keywords | join('、') }}）
{% endfor %}
请输出 JSON：conversions（数组，每项 {value, type, converted_to, why_chain}）。
```

#### 3.1.3 `career_counselor`（职业规划师）— Step 4 排序 & Step 5 工作目的

**Step 4 system prompt：**
```
你是价值观教练。基于 Step 2 的『核心价值观』与 Step 3『转化后的以自我为中心价值观』，请对价值观做优先级排序。
排序诀窍：思考『到底哪一个是我的最终目的』。
最底层 = 应优先满足的『基础』价值观，最顶层 = 人生的『最终目的』。
书中范例金字塔（底→顶）：简单→好奇心→成果→热爱→审美意识（最终目的：过着美好的生活）。
约束：
- 保留已锁定项（preserve_output.ranked 中 locked=true 的必须保留在排序中，不修改其 value）。
- 输出严格 JSON：{ "ranked": [{"value":"...","locked":false}] }（从底到顶，不含已锁定项）
```

**Step 4 user prompt 模板：**
```
【步骤2的核心价值观】
{% for g in step_2.output.groups %}- {{ g.umbrella }}
{% endfor %}
【步骤3的转化结果】
{% for c in step_3.output.conversions %}- {{ c.value }} → {{ '保留' if c.type == 'self' else c.converted_to }}
{% endfor %}
{% if preserve_output and preserve_output.ranked %}【已锁定，必须保留】
{% for k in preserve_output.ranked %}{% if k.locked %}- {{ k.value }}
{% endif %}{% endfor %}{% endif %}
请输出 JSON：ranked（数组，从底到顶，每项 {value, locked:false}）。
```

**Step 5 system prompt：**
```
你是职业规划教练。基于 Step 4 排序后的『以自我为中心价值观』与用户刚列出的『10 个向他人提供价值的经历』，请：
(1) 为每条经历标注它体现的价值观（可多选，从用户价值观中选取或自行归纳）；
(2) 统计出现最多的价值观，定为用户的『工作目的』；
(3) 给出工作目的的一句话定义与推理过程。
注意：『想让别人开心』『想让别人幸福』不是工作目的（太泛）。要落到用户真实经历中高频出现的、具体的价值观。
约束：
- 输出严格 JSON：{ "work_purpose":"...","reasoning":"...","experience_map":[{"experience":"...","values":["..."]}] }
- work_purpose / reasoning 用 markdown，150 字内。
```

**Step 5 user prompt 模板：**
```
【步骤4的价值观排序（底→顶）】
{% for k in step_4.output.ranked %}{{ loop.index }}. {{ k.value }}
{% endfor %}
【用户的10个经历】
{% for a in step_5.answers %}{{ a.answer }}
{% endfor %}
请输出 JSON：work_purpose + reasoning + experience_map（每条经历→体现价值观）。
```

#### 3.1.4 `mentor`（刘老师，全书级苏格拉底导师）— 逐点引导

> 刘老师不是第四章专属角色，而是贯穿全书的常驻导师身份。她的配置见 `../../config/mentor/mentor_config.json`，运行时由统一 Agent 的「导师模式」加载；本章通过 `mentor_hooks` 声明需要引用的跨章字段。详见 `../../docs/mentor_design_spec.md`。

**system prompt：**
```
你是用户的专属职业规划导师「刘老师」，语气像一位敏锐而温和的教练。
你精通《如何找到想做的事》全书八章：会引导用户把含糊的感受写成具体的价值观线索，并在 ch1–ch8 的完整上下文中做苏格拉底式追问。

你掌握：
- book_md（全书 8 章正文，作为方法框架与章节边界）；
- learner_profile（跨章压缩档案：values / talents / likes / work_purpose 等，≤800 tok）；
- mentor_hooks（本章聚焦点与引用字段，如 ch04 引用 values、work_purpose）；
- conversation_history（最近 8–12 轮对话记忆）。

引导原则：
- 优先基于当前章，必要时引用跨章 learner_profile 指出一致性或矛盾。
- 引用用户在各章步骤里写下的原话/关键词，指出其中蕴含的价值观或思维线索。
- 当用户卡住时，建议「书写冥想」（3分钟不停笔）或「提问对话」（找人聊）。
- 温和地做视角转换：把「我应该做」拆回「我想做」，识别虚假价值观。
- 把书中方法（可控性检验/金字塔/10经历/三要素组合/螺旋上升等）与用户实际回答连接起来，而非泛泛谈书。
- 一次只聚焦一个点，不直接给结论。

约束：
- 用中文、口语化、第二人称，像真人对话。
- 不替用户下结论，最终判断权在用户。
- 若用户要求生成分组/检验/排序/工作目的，调用对应专业角色，不要自己编造 JSON。
- 绝不把八章原始 step_runs 全量注入 prompt；只读压缩档案 + 本章钩子。
```

**user prompt 模板：**
```
【当前章节】{{ current_chapter }}（{{ current_step }}）
【跨章学习者档案】
{{ learner_profile }}
【本章导师钩子】
{{ mentor_hooks }}
【用户刚才说的】{{ user_message }}
请以刘老师身份回复，必要时追问一个具体问题。
```

---

## 4. 推荐的工作流配置（chapter_config JSON）

> ⚠️ 本节仅为**示意**，字段契约的**唯一权威来源是 `config/chapters/chapter_config_ch04.json`**（new-schema）。若本节与其不一致，**以 config JSON 为准**（与落地设计文档同口径）。完整 `few_shot_examples` / `core_concepts` / `guiding_questions` 见该 JSON；角色系统提示见 `scripts/seed_roles.py`。

```json
{
  "chapter_id": "ch04",
  "chapter_title": "找到指引人生的指南针：重要的事",
  "exercises": [
    {
      "step_id": "step-1",
      "name": "回答5个问题，找出价值观关键词",
      "input_schema": [
        {"name":"q1","label":"你尊敬的人、朋友、喜欢的角色分别是谁？你尊敬或喜欢他们哪些地方？","type":"text"},
        {"name":"q2","label":"小时候和青春期影响你最大的经历是什么？对你的价值观造成了什么影响？","type":"text"},
        {"name":"q3","label":"现在社会有什么不足？你有什么想解决的社会问题？","type":"text"},
        {"name":"q4","label":"问周围的人「你觉得我在人生中看重什么」，会得到怎样的答案？","type":"text"},
        {"name":"q5","label":"你最想给别人（孩子/后辈/朋友）什么建议？最不想告诉他们什么？","type":"text"},
        {"name":"value_questions","label":"（可选）从「重要的事 30 问」题库追加回答，作为发现素材补充（题库由 questions 服务接入，本题回答存于 book_notes，运行前由前端按 q_order 取回填入本字段）","type":"list_of_items","required":false,"item_schema":{"fields":[{"name":"question_id","type":"number","label":"题库题目全局序号 q_order（价值观分类 1–30）"},{"name":"question","type":"text","label":"题目原文（建议保存以抗题库更新）","optional":true},{"name":"answer","type":"text","label":"用户回答"}]}}
      ],
      "output_fields": [
        {"name":"top_values","type":"editable_list","lockable":true},
        {"name":"commentary","type":"markdown"}
      ],
      "step_role_id": "psychologist"
    },
    {
      "step_id": "step-2",
      "name": "形成价值观思维导图",
      "references": [{"from_exercise":"step-1","fields":["top_values"]}],
      "output_fields": [{"name":"groups","type":"editable_list","lockable":true}],
      "step_role_id": "psychologist"
    },
    {
      "step_id": "step-3",
      "name": "以他人为中心 → 以自我为中心",
      "references": [{"from_exercise":"step-2","fields":["groups"]}],
      "output_fields": [{"name":"conversions","type":"editable_list"}],
      "step_role_id": "psychologist"
    },
    {
      "step_id": "step-4",
      "name": "价值观排序（金字塔）",
      "references": [
        {"from_exercise":"step-2","fields":["groups"]},
        {"from_exercise":"step-3","fields":["conversions"]}
      ],
      "output_fields": [{"name":"ranked","type":"editable_list","lockable":true}],
      "step_role_id": "career_counselor"
    },
    {
      "step_id": "step-5",
      "name": "确定工作目的",
      "references": [{"from_exercise":"step-4","fields":["ranked"]}],
      "output_fields": [
        {"name":"work_purpose","type":"text","lockable":true},
        {"name":"reasoning","type":"markdown"},
        {"name":"experience_map","type":"table"}
      ],
      "step_role_id": "career_counselor"
    }
  ],
  "mentor_hooks": {
    "focus": "价值观与人生目的，最深一层",
    "references": ["importance"]
  }
}
```

> 注：本配置字段名（`groups` / `conversions` / `ranked` / `work_purpose`）即 §8 中提到的「前端需补渲染分支」的字段。重构时前端 `renderStepOutput` 必须处理这四类，否则 Step2/Step3 会退化成裸 JSON。

---

## 5. 数据模型与输出 schema

### 5.1 Step 1 输出
```json
{
  "commentary": "你在 Q1 反复提到『想那样生活下去的人』，Q4 身边人反馈你『简单』，几条线索都指向……",
  "top_values": ["简单", "热爱", "好奇心", "真诚", "成长"]
}
```

### 5.2 Step 2 输出
```json
{
  "groups": [
    {
      "umbrella": "简单",
      "keywords": ["最低限度", "清爽", "简单", "不冗余"],
      "locked": true
    },
    {
      "umbrella": "好奇心",
      "keywords": ["发现", "学习", "玩乐", "冲动", "成长", "真心"],
      "locked": false
    }
  ]
}
```

### 5.3 Step 3 输出
```json
{
  "conversions": [
    {
      "value": "想出名",
      "type": "other",
      "converted_to": "好奇心",
      "why_chain": ["为什么想出名？", "因为想被别人追捧", "为了认可自己的存在", "按好奇心生活", "不出名也能做到吗？能 ⇒ 好奇心"]
    },
    {
      "value": "好奇心",
      "type": "self",
      "converted_to": "",
      "why_chain": []
    }
  ]
}
```

### 5.4 Step 4 输出
```json
{
  "ranked": [
    {"value": "简单", "locked": false},
    {"value": "好奇心", "locked": false},
    {"value": "成果", "locked": false},
    {"value": "热爱", "locked": false},
    {"value": "审美意识", "locked": true}
  ]
}
```

### 5.5 Step 5 输出
```json
{
  "work_purpose": "让更多的人热爱生活。",
  "reasoning": "在 10 段经历中，『鼓励他人做喜欢的事』『传授方法让人不再迷茫』反复出现，指向『热爱』这一价值观被最高频提供给他人的经历印证。",
  "experience_map": [
    {"experience": "中学时为羽毛球部队员加油鼓劲", "values": ["热爱", "成果"]},
    {"experience": "网上鼓励他人做自己喜欢的事", "values": ["热爱"]}
  ]
}
```

- `commentary`：Step 1 映照点评（markdown）。
- `top_values`：Step 1 初步关键词。
- `groups[].umbrella`：核心价值观；`keywords`：归入的关键词；`locked`：是否锁定。
- `conversions[].type`：`other`（不可控）/ `self`（可控）；`why_chain`：5Why 追问链。
- `ranked[]`：从底（基础）到顶（最终目的）的价值观，支持锁定。
- `work_purpose`：工作目的（text）；`experience_map`：经历→价值观映射。

---

## 6. 视觉与交互规范

### 6.1 ch4 设计体系（ch5–ch8 继承的源头）

- **颜色**：
  `--paper:#f6efde / --paper-edge:#ebe1c8 / --paper-light:#faf4e3 / --ink:#2a2620 / --ink-soft:#6b6354 / --accent:#7a4d2b / --accent-bg:#f1e8d2 / --rule:#d9cfb6`
- **字体**：中文衬线栈 `"Source Han Serif SC","Noto Serif SC","Songti SC","SimSun",serif`
- **纸纹背景**：径向渐变 vignette，全局统一。
- **赭石 accent**：用于步骤高亮、锁定图标、金字塔顶层。

### 6.2 第四章专属视觉元素

- **Step 2 思维导图卡片**：纸张卡片，每张显示 umbrella 标题 + 关键词 chips；chips 可点击删除，卡片可锁定 / 删除；分组间用虚线连接暗示「归纳关系」。
- **Step 3 可控性检验**：每行一个 value，左侧状态徽章（other=灰蓝「他人」/ self=赭石「自我」）；`type=other` 项展开 5Why 追问链（竖向步骤条，节点显示「为什么…」→「因为…」）。
- **Step 4 金字塔**：纵向金字塔（或分层列表），底层宽、顶层窄；顶层标注「最终目的」、底层标注「基础」；可拖拽排序；锁定项显示 🔒。
- **Step 5 工作目的卡片**：顶部固定，纸张卷轴样式，可导出图片；下方 `experience_map` 以「经历 → 价值观标签」成对展示。
- **100 例清单 / 30 问抽屉**：同 ch5 风格，可搜索、可引用关键词到 Step 2。

### 6.3 动效与反馈

- 提交 Step 1 后，Step 2 卡片从折叠状态微动展开提示。
- Step 3 的 5Why 链逐节点展开（一次一个 why），呼应「追问」节奏。
- 锁定 / 解锁时图标 150ms 缩放反馈。
- Step 5 工作目的确定后，顶部卡片有「落地」淡入动效。
- **修改已完成步骤的提醒弹窗**：用户点击已 `submitted` 步的「修改」时弹出确认（见 §1.8），强调下游失效与档案清空风险，防止误改；末步弱化。

---

## 7. 验收标准

- [ ] 第四章正文渲染完整，图 4-1～4-10 文字说明正确呈现，文字可选中。
- [ ] 左侧步骤导航显示 Step 1–5 共 5 个步骤，顺序与书本一致。
- [ ] Step 1 的 5 个问题可独立保存草稿、整体提交；LLM 输出 `commentary` + `top_values`。
- [ ] Step 2 可手动增删分组卡片，支持 umbrella 编辑、关键词 chips、`🔒` 锁定；LLM 生成 `groups` 且锁定项保留。
- [ ] **Step 2 的 `groups` 在前端有专门渲染分支（非裸 JSON）** —— 重构必须项。
- [ ] Step 3 正确标注 `type`（other/self），`type=other` 展示 5Why `why_chain`；LLM 生成 `conversions`。
- [ ] **Step 3 的 `conversions` 在前端有专门渲染分支（非裸 JSON）** —— 重构必须项。
- [ ] Step 4 可拖拽排序、`🔒` 锁定；金字塔底=基础 / 顶=最终目的；LLM 生成 `ranked` 且锁定项保留。
- [ ] Step 5 能列出 10 经历并生成 `work_purpose` + `reasoning` + `experience_map`；工作目的卡片可导出。
- [ ] 100 例清单 / 30 问抽屉可正常展开、搜索、引用。
- [ ] 导师对话能引用各步内容做个性化回应（刘老师融合设计）。
- [ ] 笔记、目录、进度、摘要、对话等通用功能与 ch5–ch8 行为一致。
- [ ] 对已 `submitted` 的 step 点击「修改」弹出确认提醒（说明下游失效 / 档案清空风险），末步弱化；上游步重提交后下游步显示「已过期，请重新运行」而非「已完成」（见 §1.8）。

---

## 8. 待确认事项

1. **前端 `renderStepOutput` 必须补 `groups` / `conversions` 分支**（最高优先级）。当前硬编码字段只有 `keywords / comments+improvements / ranked / work_purpose / commentary`，Step2/Step3 会退化裸 JSON。建议在本轮重构一并修复。
2. **Step 1 是否拆成 5 个独立步骤**？已拍板：第四章 = 5 个独立 step（exercise）级联（step-1 5问 → step-2 思维导图 → step-3 可控性转化 → step-4 金字塔 → step-5 工作目的），与 new-schema `references.from_exercise` 级联一致，非「1 步 5 问同屏」（旧 seed_ch4.py 表述已作废）。
3. **Q5 的「最不想告诉别人什么」** 是否也传给 LLM 作为反向线索？建议「是」，能提升工作目的准确性。
4. **`work_purpose` 是否允许用户在 Step 5 手动覆盖 LLM 建议**？建议允许，LLM 仅给建议。
5. **刘老师对话是否跨步骤记忆**？本 PRD 设计 `user_progress` 注入 system，建议 chat.py 查询 step_runs 组装后注入。
6. **ch4 是否补序 ch00（全书导言）**？当前从 ch4 起，前言章节待定。

---

## 9. 与 Ardot 画布的衔接

本 PRD 完成后，下一步是在 Ardot 中新建设计稿并绘制第四章高保真界面。操作方式：
1. 在 WorkBuddy 中确认 Ardot 编辑器已打开或新建一个 `.ardot` 文件。
2. 将文件 ID 或链接发给我，我通过 `open_design` 连接画布。
3. 我按本 PRD 绘制：三栏布局、Step 1 五问卡、Step 2 思维导图分组卡、Step 3 可控性 5Why 链、Step 4 金字塔、Step 5 工作目的卡、100 例 / 30 问抽屉、导师对话面板。

> 注：此前已产出 4 张相关 Ardot 图（云端 fileId）：LLM 提示词架构图（710630790445620）、章节任务配置模板（710631995475007）、LLM 运行时数据流图（710635388127241）、第四章 LLM 设计修正与导师融合（710643309154221），可作为视觉与提示词架构参考。

---

## 10. 参考文件

- 原书 PDF：第 99–137 页（正文章节「重要的事」）
- 原书第四章 HTML：`backend/book/.../04-important.html`（或 mineru 提取版）
- 原书第四章 MD：`chapter_md/第四章-找到指引人生的指南针：重要的事.md`
- 后端权威配置（new-schema）：`../../config/chapters/chapter_config_ch04.json`（由 `seed_all_chapters.py` 灌入 `chapter_configs` 表）；角色系统提示见 `../../scripts/seed_roles.py`。`seed_ch4.py` 为旧架构遗留，不另作权威源。
- 上下文层配置（统一 Agent 架构）：`../../config/chapters/chapter_config_ch04.json`（core_concepts / mentor / few_shot）
- 题库权威源：`chapter_md/问题清单.md`（重要的事 30 问 + 100 例清单）
- 平台总 SPEC：`SPEC.md`
- 后续章节 PRD：`PRD-ch5.md`、`PRD-ch6.md`、`PRD-ch7.md`、`PRD-ch8.md`
- 设计原型：`platform/prototype/ch04_exercise_mockup.html`
