# ch04 双专家评审记录（2026-08-09 重跑）

> 评审方式：架构评审 + 产品/UX 评审两个**只读** agent 并行，基于当前 ch04 四份交付物（config / PRD / 落地设计 / mockup）重跑。
> 整改归属原则（依 2026-08-09 权责界定）：**设计侧** = config / PRD / 落地文档 / mockup（页面逻辑与数据库设计）；**开发侧** = 运行时代码（assembler / steps / agent / compaction）、数据库 reseed。开发侧事项设计侧只在文档指出、请开发处理，不动代码。

## P0（阻断项）

### P0-1 架构｜config 缺 `instruction` 字段 —— 设计侧已修
- **问题**：schema 第 111 行 `required: ["name","instruction"]`，但 ch04 五个 exercise 仅用 `method`+`output`，无 `instruction`。`assembler.py:421` 只读 `exercise.get("instruction","")`，导致 `[this exercise]` 块恒空，LLM 拿不到步骤指引，step-2/3/5 的嵌套产出形状（umbrella/keywords/locked、value/type/converted_to/why_chain、experience_map）注入缺失。
- **整改**：给 5 个 exercise 各补 `instruction`（合并 method+output 语义，键名对齐 ch01/02/03/05/06/07/08）。config JSON 已校验，5/5 齐全。
- **证据**：`chapter_config_ch04.json` 各 step；`config/schema/chapter_task_config.schema.json:111`；`platform/backend/app/runtime/assembler.py:421`。

### P0-2 架构｜`preserve_output`（锁定项保留）在新架构被静默丢弃 —— 开发侧待办
- **问题**：`steps.py:45` 定义了 `RunIn.preserve_output`，但 `_run_step_option_b`（:252-268）只传 `user_answers`；`agent.run_step` / `build_step_prompt` 均无该参数；仅遗留 `step_runner.py` 支持。PRD / 落地文档 / mockup 中"重生成保留锁定项"无真实实现，`lockable:true` 的 groups/ranked 重生成即丢失。
- **整改归属**：运行时代码，归开发。两种修法：(a) 补 agent/assembler 的 preserve 透传（则文档"零代码改动"表述需同步改）；(b) 文档+原型统一改口为"锁定 = 整步提交态，重生成不保证保留"。建议开发侧定方案。
- **设计侧已做**：mockup 用可见重排 + toast 区分"锁定保留"反馈（shuffleUnlocked 保留 locked 项位置），但真实持久保留仍依赖后端实现。

### P0-3 UX｜原型书名错误 —— 设计侧已修
- 《你要如何衡量你的人生》→《如何找到想做的事》（PRD:241/370 为正确书名）。已改。

### P0-1 UX｜金字塔上下倒置 —— 设计侧已修
- 原实现首项（底层·基础）渲染在最上、末项（顶层·最终目的）在最下，与 PRD §6.2"顶=最终目的"直觉相反。已改为**视觉顶部 = 最终目的、底部 = 基础**；按钮文案改"↑ 更基础 / ↓ 更靠目的"，语义对齐 PRD。

### P0-2 UX｜step-2 关键词门槛形同虚设 —— 设计侧已修
- 原只显示"来自 step-1 的 N 个关键词"且 ≥15 直接放行，100 例抽屉入口只在 step-1。已加 **N/15 实时计数** + 不足 15 高亮"查 100 例补充"；step-2 加"查 100 例"入口，抽屉可引用到 step-2（`addScratch` 支持 `scratchItems2`）。

### P0-3 UX｜三处"重新生成"空反馈 —— 设计侧已修
- 原点击"重新生成"仅弹 toast 用同一份数据重渲染，界面零变化（欺骗性反馈）。已加 `shuffleUnlocked` 助手：未锁定项产生可见重排，锁定量写入 toast（"已按 N 个锁定项保留，其余重新分组"等）。

## P1（应修项）

### P1-4 架构｜step-5 输入类型漂移（list_of_items vs 单 textarea）—— 待对齐
- config = `list_of_items` + `min_items:10` + `item_schema.experience`；PRD / 落地 / mockup 此前写"单 textarea 按行拆分"，后端 `steps.py` 对非 list 直接 422。
- **归属**：config 为权威契约。设计侧已在 mockup 用多行 textarea + 行数校验（≥10），与 config 语义等价；建议 PRD / 落地明确"前端按行转 `[{experience}]`"以消除表述漂移。属于文档措辞对齐，非阻断。

### P1-5 UX｜引导问题与 config 不符 —— 设计侧已修
- mockup 原 3 条引导问题中 2 条不在 config（其中"别人常夸你/爱向你请教"属 ch05 擅长语义）。已改为直接取 config `guiding_questions` 三条。

### P1-6 架构｜`values`/`ranked` 非跨章保护字段 —— 开发侧待办
- `compaction.py:38-40` 的 `MANDATORY_DOWNSTREAM_FIELDS` 只含 `work_purpose`，`_trim_to_budget`（:81-97）超 800tok 时可裁掉 `values`/`ranked`；落地文档承诺"保值到 ch7/ch8"。
- **归属**：compaction.py 代码，归开发。建议把 `values`/`ranked` 加入 MANDATORY 集，或在文档加"预算超限可能降级"注脚。

### P1-7 UX｜演示数据未忠实级联 —— 设计侧已修
- `DEMO_TOP_VALUES` 扩到 15；`DEMO_GROUPS` 4 组共 15 词；`DEMO_CONVERSIONS` 仅用 groups 中存在的 value（删除"让父母骄傲/被很多人喜欢"等不在任何 umbrella 的外部项）；`DEMO_RANKED` 与 groups 对齐。step-1→2→3→4→5 数据可追溯。

### P1-7(2) UX｜工作目的卡双份文案 —— 设计侧已修
- 原 `wp-text` 与预填 textarea 并存、placeholder 永不出现。已合并为单一可编辑 textarea（"可直接修改，AI 的建议只是起点"）。

### P1-8 UX｜文案黑话 —— 设计侧已修
- saved-state 去掉"work_purpose 供 ch7 过滤器、ranked/values 供 ch8 引用"字段名，改业务口径（"这句话会在第七章帮你筛方向、在第八章陪你落地实践"）；`DEMO_REASONING` 去掉"如上"。

## P2（非阻塞，记录备查）
- 落地文档"5 个 exercise 级联"与"ch03 是 1 exercise 4 阶段"表述并存——实为对比说明，非矛盾。
- `compaction` 仅末步触发（`steps.py` "if last step"），落地 §8.4 表述"每次 submit 触发"——建议措辞改为"末步确认时沉淀"。
- `mentor_hooks.references` 同时被 `assembler` 用作每步 profile 注入键，落地 §8 称"仅作开场回响"不够准确——待开发侧确认 assembler 用法后统一表述。
- PRD 编号重复（§1.7 重复）、落地引"PRD §1.6"指错——文档级小勘误，建议顺手修。
- mockup step-2 缺"删除分组"按钮（已有＋加关键词）；100 例抽屉搜索无空态——体验增强。
- 5Why 未逐节点展开（一次性渲染）——PRD 要求一次一个 why，正式前端实现。
- 进入 step-5 进度即 100%——进度条语义微调。
- 对话 tab 为静态气泡无输入框——正式前端接 mentor 服务后实现；导师引用 ch3（importance）正确，保留。

## 测试实跑
`test_context_integrity.py` + `test_config_consistency.py` + `test_m4_ch04_cascade.py` = **26 passed**（与基线一致；现有测试不覆盖 P0-1 instruction / P0-2 preserve_output）。

## 开发侧待办清单（请开发处理）
1. **assembler.py**：确认步骤 prompt 注入 `instruction`（design 侧已补 config，若仍依赖 method/output 需改读取逻辑）；`mentor_hooks.references` 每步注入用法厘清。
2. **steps.py / agent.py / assembler.py**：`preserve_output` 透传（P0-2），使 lockable 项重生成真实保留。
3. **compaction.py**：`values`/`ranked` 加入 `MANDATORY_DOWNSTREAM_FIELDS`（P1-6）。
4. **数据库 reseed**：ch04 config 已改（加 instruction），待开发重灌 DB 以消除 consistency 脚本报告的 ch04 DRIFT。
