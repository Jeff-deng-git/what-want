# ch05《擅长的事》开发 Review：待设计确认

> 创建日期：2026-08-11  
> Review 对象：`D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md`  
> Review 结论：**暂不能直接进入 ch05 交付**。业务流程已经基本清楚，但仍存在权威文档冲突、字段契约不完整和上下文边界未完全确定的问题。

## 1. Review 范围与依据

本次 review 对照了以下内容：

- `D:\AI_Project\What_Want\llm_prompt_design\docs\工程实施交接文档.md`
- `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md`
- `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md`
- `D:\AI_Project\What_Want\llm_prompt_design\config\chapters\chapter_config_ch05.json`
- `D:\AI_Project\What_Want\llm_prompt_design\docs\mentor_design_spec.md`
- `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_专家评审记录.md`
- 当前后端运行时、步骤路由、profile compaction、前端通用步骤 renderer

当前 review **没有修改代码、配置、数据库，也没有重启 3011/8011 服务**。

## 2. 总体结论

新版 ch05 的核心流程是明确的：

1. step-1：用户回答书中 5 个问题，LLM 映照并归纳初步 `talents`；
2. step-2：用户审阅、评级、增删和锁定长处，LLM 生成正式 `talents` 与《使用说明书》；
3. step-2 的 `talents` 写入 `learner_profile`，供 ch07 使用；
4. 导师通过 `mentor_hooks.references=["work_purpose"]` 建立 ch04 → ch05 连续性。

但是，以下问题不确认，开发侧无法保证数据结构、重生成、导师上下文和跨章节引用的一致性。

## 3. P0：必须先确认的设计问题

### 3.1 ch05 的最终流程是否正式从“九宫格 1 步”切换为“5 问 2 步”

当前存在明确的权威文档冲突：

- 工程实施交接文档仍定义 ch05 为“九宫格擅长梳理”单步：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\工程实施交接文档.md:297-306`
- 新版 ch05 落地设计定义为“2 个真实 step”：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md:19-25`、`:30-36`
- 新版 PRD 也已改为“回答 5 问 → 评级 + 使用说明书”：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md:15-27`

由于工程实施交接文档被定义为唯一权威设计依据，请设计侧明确确认：

> 是否正式废弃交接文档 §7.5 的旧九宫格方案，采用新版 ch05 两步方案，并同步更新交接文档、相关总览和测试契约？

如果确认采用新版，建议同步修改交接文档至少包括：

- step 数量：1 → 2；
- step-1 输入：九宫格 → 5 个固定问题；
- step-1/step-2 输出字段；
- 角色：旧配置中的 `career_counselor` → 新配置中的 `psychologist`；
- ch07 对 `talents` 的生产者从 ch03 初版转为 ch05 正式版。

### 3.2 `reference_strengths` 是否进入最终 `talents`

当前文档存在两种可能解释：

- 配置将 `reference_strengths` 定义为 step-2 的独立用户输入：
  `D:\AI_Project\What_Want\llm_prompt_design\config\chapters\chapter_config_ch05.json:66-77`
- Prompt 要求把 step-1 长处和参考长处合并去重后生成 10+ 长处：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md:230-239`
- 专家评审记录又写明 `reference_strengths` 应独立提交、不混入 `talents`：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_专家评审记录.md:45-46`

请确认以下具体规则：

1. 100 例清单引用项是否只是给 LLM 的参考素材，还是允许进入最终 `talents`？
2. 如果进入最终 `talents`，是否需要保留来源字段，例如 `source: "100_examples"` 或 `from_example: true`？
3. 如果不进入最终 `talents`，LLM 是否仍可以根据它重新表达出一个与用户素材相关的长处？
4. “已收集 N / 10 个长处”的 N，按什么计算：
   - step-1 `talents`；
   - 最终 step-2 `talents`；
   - `step-1 talents ∪ reference_strengths` 去重结果；
   - 还是包含用户手动新增项的当前编辑列表？
5. 去重是精确字符串去重，还是允许语义相近项合并？由前端、后端还是 LLM 完成？

### 3.3 `talents` 的 canonical 数据结构

当前 step-1 与 step-2 的输出结构不同：

- step-1 Prompt 要求输出字符串数组：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md:209-213`
- step-2 Prompt 要求输出对象数组：
  `{text, rating, locked}`，见 `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md:236-238`
- `chapter_config_ch05.json` 只声明 `type: editable_list`，没有声明 item schema：
  `D:\AI_Project\What_Want\llm_prompt_design\config\chapters\chapter_config_ch05.json:35-45`、`:84-94`

请确认是否采用以下标准：

```json
{
  "text": "具体长处",
  "rating": "◎|〇|△|空字符串",
  "locked": false
}
```

并明确：

- step-1 是否可以继续输出 `string[]`；
- 进入 step-2 时是否由运行时标准化为对象数组；
- step-1 的 `locked` 是否需要存在；
- `rating` 是否允许为空；
- profile 中最终保存的是否必须始终是对象数组；
- 是否允许保留 `source/from_example` 等额外字段。

建议将该 item schema 写入配置 schema 或 ch05 config，而不要只放在 Prompt 文本中。

### 3.4 “单项锁定”与“整步提交锁定”的关系

设计一方面要求每项长处支持锁定，另一方面又在 §8.4 定义“真实锁定 = 整步进入 submitted”：

- 单项可锁定、重生成时保留：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md:62-65`
- 整步 submitted 视为锁定：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md:129-134`

请明确下面的状态规则：

1. 用户在提交前锁定单项，是否只影响下一次 LLM 重生成？
2. 重生成时，已手动评级但未锁定的项目是否允许被覆盖？
3. 用户解锁后是否允许重新生成覆盖该项？
4. step submitted 后是否永久不可编辑？
5. 如果需要修改已提交结果，是新建 run，还是允许在原 run 上修改？
6. 新 run 生成后，旧 submitted 结果是否继续作为 profile 唯一有效版本，直到新 run 再次 submitted？
7. 锁定项是否必须保留原位置？当前运行时的 `merge_preserved_output` 按原位置保留，设计侧应确认这是 ch05 的正式规则。

### 3.5 《使用说明书》是否允许用户编辑

这里存在直接冲突：

- PRD 写为“只读，LLM 生成，用户可微调文本”：
  `D:\AI_Project\What_Want\llm_prompt_design\docs\PRD\PRD-ch5.md:133-140`
- config 与落地设计声明 `user_manual` 为 `readonly: true`：
  `D:\AI_Project\What_Want\llm_prompt_design\config\chapters\chapter_config_ch05.json:90-94`
  `D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md:62-65`

请二选一：

- A：只读，只能重新运行 LLM；
- B：LLM 生成建议，用户可以编辑并保存。

如果选择 B，需要同时明确：

- 编辑结果是否进入 profile；
- 编辑结果是否参与后续导师上下文；
- 用户编辑后的文本是否在下一次 LLM 重生成中作为 `preserve_output` 传入；
- “提交”后是否再次锁定。

### 3.6 ch05 导师读取的上下文边界

新版设计写的是导师读取本章各步答案/输出：

`D:\AI_Project\What_Want\llm_prompt_design\docs\ch05_落地级步骤设计.md:67-71`

但工程实施交接文档规定，章节内后续上下文只能使用已提交的 `step_runs.parsed_output`，不能把草稿或未提交结果当作后续输入：

`D:\AI_Project\What_Want\llm_prompt_design\docs\工程实施交接文档.md:477-487`

当前实现中，非 ch04 章节的导师桥接只读取指定的一个 submitted step：

- `D:\AI_Project\What_Want\platform\backend\app\routers\chat.py:93-109`
- `D:\AI_Project\What_Want\platform\backend\app\routers\chat.py:192-197`

请确认：

1. 导师是否严格只读 submitted 输出？建议遵守全局硬规则，不读取草稿和未提交 LLM 输出。
2. 用户在 step-2 运行 LLM 但尚未提交时，导师是否应能看到 step-1 已提交输出？
3. 导师进入 step-2 时，是只注入当前 focus step，还是注入本章全部已提交步骤？
4. `work_purpose` 是否只作为导师钩子使用，绝不进入 step-1/step-2 的普通分析 Prompt？

## 4. P1：建议补充的行为规则

### 4.1 “10+ 长处”是目标还是硬性输出要求

设计同时要求：

- 目标为 10 个以上、理想 20 个；
- LLM 不得臆造用户没有透露的长处。

需要补充异常路径：

- 如果 5 问素材不足以支持 10 个长处，LLM 是否返回少于 10 个？
- 是否允许继续进入 step-2？
- step-2 是否允许少于 10 个时提交？
- 页面是仅显示警告，还是必须补充 30 问/100 例后才可继续？

建议将“10+”定义为用户引导目标，而不是 LLM 为满足数量而补写的硬约束；不足时应返回已有证据并提示补充素材。

### 4.2 ch05 专属 Prompt 的实际落点

PRD §3.1 描述了 ch05 专属的 psychologist Prompt，但当前运行时实际读取的是 DB 中全局 `psychologist` 角色，再由 assembler 注入章节 MD、core concepts、exercise instruction、few-shot 和 output fields：

- `D:\AI_Project\What_Want\platform\backend\app\runtime\agent.py:218-252`
- `D:\AI_Project\What_Want\platform\backend\app\runtime\assembler.py:486-631`
- `D:\AI_Project\What_Want\platform\backend\scripts\seed_roles.py:44-48`

请确认以下实现契约：

- 全局 psychologist + config 的 instruction/core/few-shot 是否足以承载 ch05 §3.1 约束；
- 还是需要新增 chapter-specific prompt/contract 字段；
- step-1、step-2 是否必须分别加入“不替用户定性”“不臆造”“建议评级但不代替用户评级”等硬约束。

不能只把约束写在 PRD 中而不明确其运行时注入位置，否则文档和真实 Prompt 会再次漂移。

## 5. 已确认的实现阻塞项（不属于设计疑问）

以下问题已经可以从当前代码直接确认，设计侧无需重新决策，但进入交付计划时必须处理。

### 5.1 DB active config 仍是旧九宫格

执行：

```text
python scripts/check_config_consistency.py --chapter ch05
```

当前结果：

```text
[DRIFT] ch05  drift=core_concepts,exercises,few_shot_examples,guiding_questions,mentor_hooks,output_template  missing_in_db=step_context,ui_context
        exercises_count: file=2 db=1
```

说明：JSON 已是新版，但运行时 DB 仍是旧版九宫格。进入测试前必须 reseed/publish，并重新执行一致性检查。

### 5.2 前端通用 editable list 不支持 ch05 数据结构

当前通用 renderer 假设列表项使用 `value` 字段：

`D:\AI_Project\What_Want\platform\frontend\index.html:2497-2518`

而 ch05 step-2 设计使用 `{text, rating, locked}`。直接复用会导致：

- 长处文本显示为空或显示异常；
- ◎〇△ 评级控件不存在；
- 保存时可能把 `text` 转成 `value`；
- `rating` 字段可能丢失；
- 锁定、删除、重生成行为与设计不一致。

必须增加 ch05 专用 renderer 或扩展通用 renderer，并补充前端契约测试。

### 5.3 ch05 的 preserve_output 当前不会被前端正确透传

前端 `runStep()` 仅通过旧版 `llm_op.output_schema.type === "editable_list"` 判断是否发送 `preserve_output`：

`D:\AI_Project\What_Want\platform\frontend\index.html:2588-2607`

而新版 config 经 `main.py` 转换后，`llm_op.output_schema.type` 是 `structured`：

`D:\AI_Project\What_Want\platform\backend\app\main.py:67-84`

因此 ch05 重跑时，前端不会自动携带已编辑/锁定的 `talents`，后端虽然支持 `preserve_output`，实际链路仍不完整。

### 5.4 当前测试与新版设计仍不一致

现有测试仍按旧版 ch05 断言 `career_counselor`、单步和旧输出结构：

- `D:\AI_Project\What_Want\platform\backend\tests\test_m4_per_chapter.py:51-55`
- `D:\AI_Project\What_Want\platform\backend\tests\test_all_chapters_e2e.py:21`

需要更新为 `psychologist`、2 steps、step-1/step-2 输出契约，并新增 ch05 级联测试，至少覆盖：

- step-1 submitted → step-2 才能读取 `talents`；
- 未 submitted 时 step-2 返回上下文错误；
- step-2 `reference_strengths` 独立存储；
- 锁定项保留原位置；
- `commentary`/`user_manual` 不进入 profile；
- 最终 step-2 `talents` 覆盖 ch03 初始 `talents`。

### 5.5 ch07 的 talents producer 仍登记为 ch03

当前跨章 gating 仍是：

`D:\AI_Project\What_Want\platform\backend\app\runtime\context_requirements.py:10-15`

设计上 ch05 才是带 ◎〇△ 评级的 `talents` 权威生产者。ch07 后续实现前，需要把 producer/consumer 契约改成：

```text
ch05.talents -> ch07.talents
ch04.work_purpose -> ch07.work_purpose
```

否则 ch07 可能继续错误地把 ch03 初始 `talents` 当成正式输入。

## 6. 建议设计侧回复格式

建议设计侧逐条回复本文件中的问题，至少明确：

```text
3.1：确认采用新版 2-step；同步更新交接文档 §7.5。
3.2：reference_strengths 只作为输入参考 / 或进入最终 talents；N 按……计算。
3.3：talents canonical schema 为……。
3.4：单项锁定与 submitted 的关系为……。
3.5：user_manual 选择 A / B；编辑结果……。
3.6：导师只读 submitted；step-2 对话注入……。
4.1：10+ 是引导目标，不足时……。
4.2：ch05 Prompt 通过……注入运行时。
```

## 7. 进入实施的前置条件

满足以下条件后，开发侧即可开始 ch05：

1. 设计侧确认并回填第 3、4 节问题；
2. 权威交接文档与 ch05 PRD/config 统一为同一方案；
3. `reference_strengths`、`talents`、`locked`、`rating`、`user_manual` 的契约固定；
4. 明确导师只读取哪些已提交上下文；
5. reseed ch05 active config，`check_config_consistency.py --chapter ch05` 返回 CLEAN；
6. 开发实施前先建立 ch05 专用契约测试，再改前端 renderer 和运行时；
7. 完成后用 Git 保存可回滚版本，并执行 ch05 后端 + 前端 E2E 验收。