# ch06 落地级步骤设计：开发侧复核 → 设计侧回复

> 审核对象：`docs/ch06_落地级步骤设计_待设计确认.md`（开发侧复核，2026-08-12）
> 设计侧回复日期：2026-08-12
> 审核依据：`config/chapters/chapter_config_ch06.json`（运行时唯一权威）／ `docs/ch06_落地级步骤设计.md` ／ `docs/PRD/PRD-ch6.md` ／ `config/chapters/chapter_config_ch07.json`
> 本文为设计侧对开发侧复核的逐条回复与拍板；**冲突以 `chapter_config_ch06.json` 为准**。

---

## 0. 总体结论

1. 开发侧对 ch06 现状的整体判断——**config 两步方向正确、生产实现尚未闭环、不能视为已交付、ch05/ch06 交付边界必须分开**——设计侧**完全认同**，无需补充。
2. 开发侧标记的大多数 P0/P1 属于「实现缺口，设计契约已明确」，设计侧**无异议**，按契约补齐即可，无需改设计。
3. 但开发侧文档**自身有 3 处前提/事实需纠正**（§14 ch07 门禁、§14 误列 ch03、§6 对 canonical 归属的表述）；另有 **2 处是设计侧自己的文档/配置 bug**，本次已一并修复（见 §2）。
4. 开发侧提的 5 个最小问题，设计侧全部拍板（见 §1）；其中第 2、4、5 项涉及设计侧契约/文档改动，本次已落盘。

---

## 1. 五个最小问题的设计侧拍板（对应开发侧 §15）

**Q1 — `aspects`/`linked_strengths` 的 canonical payload：对象数组还是字符串数组？**
→ **对象数组 `[{text}]`**（开发侧方案 A）。
依据：`chapter_config_ch06.json` 中两者均声明为 `editable_list` + `item_schema: {fields:[{name:text}]}`，与 ch05 `reference_strengths` 同构。
**注意**：开发侧原 §15 描述「config 声明对象数组、但落地/PRD 示例是字符串数组」属实——那是设计侧文档示例的 bug，本次已将 `落地 §8.2` 与 `PRD §6.2` 的字符串数组示例统一改为对象数组（见 §2）。开发实现**以 config 为准**。

**Q2 — 100 例引用的领域 seed 是否新增 `passion_seeds` scratchpad？**
→ **是，方案 A（开发侧推荐），且已写入 config。**
已在 `chapter_config_ch06.json` 的 step-2 `input_schema` 增加：
```json
{ "name": "passion_seeds", "type": "list_of_items", "required": false,
  "item_schema": { "fields": [ {"name":"text","type":"text"}, {"name":"source","type":"string","default":"passion_examples"} ] } }
```
规则：仅持久化到 `user_answers`；运行时与 step-1 `likes`、`reference_talents` **合并去重**作为 LLM 分析上下文；**不写入最终 `likes`**（避免污染下游 ch07）。

**Q3 — 30 问抽屉是否用现有 `/api/questions?category=热情`？选中后写入哪里？**
→ **确认开发侧建议**：
- 固定使用现有题库 API（`/api/questions?category=热情`，由 `seed_questions.py` 接 `问题清单.md`），不另造接口；
- 选中题目后**打开回答编辑区**，**先持久化到独立 scratchpad**，**不自动调用 LLM**；
- 运行时明确拼入 user evidence（Step 1 的 q1–q5 证据 / Step 2 的灵感 seed）；
- 题库服务正式接入为开放点（见 §6），原型阶段可用 demo 数组。

**Q4 — `ch04 work_purpose` 依赖是否移除？**
→ **先移除（开发侧推荐）。**
`grep` 验证：`chapter_config_ch06.json` 全文**无** `work_purpose`/`Q27` 任何钩子（`step_context`/`mentor_hooks`/`ui_context` 均无）。原 `落地 §5` 的「ch4 work_purpose｜仅 Q27 类轻提示」是**产品设想、无运行时契约**，本次已降格（见 §2）。ch06 现阶段**不消费 `work_purpose`**；待跨章提示机制独立定义后再补。

**Q5 — 是否替换 few-shot 中「书里说」表述？**
→ **是（开发侧推荐），且已修复。**
`chapter_config_ch06.json` step-2 `few_shot_examples` 原 assistant 含「书里说『喜欢的事』必须拆到『方面』…」，已改写为「从方法上看，『喜欢的事』要拆到『方面』才能和『擅长的事』连起来…」，保留方法说明、去除「书里说/作者说」口吻。开发侧重新 reseed 后即生效。

---

## 2. 设计侧已自行修复的 bug（对应开发侧 §6 / §13 / §5 决策 / §7 决策）

| 项 | 文件 | 修复 |
|----|------|------|
| §6 canonical 形态 | `docs/ch06_落地级步骤设计.md` §8.2 | 示例 `aspects/linked_strengths` 由字符串数组改为对象数组 `[{text}]`，对齐 config |
| §6 canonical 形态 | `docs/PRD/PRD-ch6.md` §6.2（两处示例） | 同上，字符串数组 → 对象数组 |
| §13 few-shot「书里说」 | `config/chapters/chapter_config_ch06.json` | step-2 few-shot 改写，去除「书里说」 |
| §5 决策（passion_seeds） | `config/chapters/chapter_config_ch06.json` | step-2 `input_schema` 新增 `passion_seeds` carrier |
| §5 决策（passion_seeds） | `docs/ch06_落地级步骤设计.md` §4.3 / §6 / §8.6 | 显式命名 `passion_seeds`，消除「草稿卡片（领域 seed）」无字段名模糊表述 |
| §7 决策（work_purpose） | `docs/ch06_落地级步骤设计.md` §5 | 该行降格为「产品设想，无运行时契约」，明确 ch06 不消费 `work_purpose` |

> 改前已按「改前必须备份」规则备份三份原文件至 `docs/backups/`（后缀 `pre-devreply-2026-08-12`）。

---

## 3. 必须纠正的开发侧前提（重要）

**P-纠正-1：开发侧 §14「当前 ch07 上游门禁已有 ch03/ch04/ch05，但没有 ch06 likes」不成立。**
依据当前的 `chapter_config_ch07.json`（2026-08-12 P0 修复后）：
- `step_context.references = ["talents","likes","work_purpose","values"]`
- `mentor_hooks.references = ["talents","likes","work_purpose","values"]`

即 **ch07 已经消费 ch06 `likes`**（作为「喜欢 × 擅长」组合的原料）。开发侧 §14 的判断基于 ch07 旧稿，请**不要重复添加**「ch07 缺 ch06 likes」的依赖；ch07 的 ch06 上游门禁已由设计侧在 ch07 设计阶段收口。

**P-纠正-2：开发侧 §14「ch07 上游含 ch03」有误。**
ch07 的正确上游是 **ch04（values/work_purpose）+ ch05（talents）+ ch06（likes）**，**不含 ch03**。ch03 是「内外部动机比/ reclaim 项」种子章，不进入 ch07 消费链。

**P-纠正-3：开发侧 §6 把 canonical 归属说反了。**
正确表述应是「config 权威为对象数组 `[{text}]`；落地 §8.2 与 PRD §6.2 的字符串数组示例是文档 bug（本次已修）」，而非「config 与文档不一致、待统一」。统一方向以 **config 为准（对象数组）**，开发实现无需二选一。

---

## 4. 对开发侧全文的逐节回应

| 开发侧节 | 结论 | 设计动作 |
|----------|------|----------|
| §0 终审 | 认同（另注：config 现已含 `passion_seeds`） | 无 |
| §1 交付边界 | 认同，git 分开建议采纳 | 无 |
| §2 已确认部分 | 基本认同；`work_purpose` 已从可执行契约移除（见 §7） | 无 |
| §3 P0 后端校验 | 实现缺口，契约已明确 | 设计确认：canonical = 对象数组 `[{text}]`；dev 按 config 补 `_is_ch06_step` + normalize/validate |
| §4 P0 assembler 合并块 | 实现缺口，契约已明确 | 设计确认：canonical `[{text}]`；与 step-1 领域、`passion_seeds` **合并去重**（trim + 连续空白归一化 + 精确去重）；**不写入 `likes`**；`linked_strengths` 由 LLM 写入 likes 子项 |
| §5 P0 100 例 seed | 设计拍板方案 A | **已写入 config**（`passion_seeds`）；dev 实现载体 + 合并 + 持久化 |
| §6 P1 嵌套 canonical | 方案 A 对象数组 | **已修文档示例**（§2）；dev 以 config 为准 |
| §7 P1 work_purpose | 先移除 | **已降格落地 §5**（§2）；ch06 不消费 |
| §8 P1 30 问 | 确认 dev 建议 | 设计确认契约；题库接入为开放点 |
| §9 P1 mentor loader | 实现缺口 | 设计确认：mentor 只读 submitted，draft/saved 不进入；ch06 切 submitted-only loader + 反向测试 |
| §10 P1 renderCh06StepBody | 实现缺口 | 设计确认：新增专属 renderer，不动通用 |
| §11 P1 passion_examples 传前端 | 实现缺口 | 设计确认：配置声明驱动安全白名单（strength_examples + passion_examples），不硬编码、不注入 step prompt |
| §12 P1 草稿持久化 | 实现缺口 | 设计确认 §8.6（含 `passion_seeds` 草稿）；需刷新/重入恢复 + 失败提示 + 隔离测试 |
| §13 P1 few-shot | 设计已修 | **已改 config**（§2）；dev reseed 生效 |
| §14 P2 ch07 消费 | 前提错误 | **见 §3 纠正**；ch07 已含 ch06 likes，勿重复添加 |

---

## 5. 开发侧待办清单（设计契约已定，dev 实现）

**契约层（config 已定，dev 消费）**
- reseed 消 DRIFT：`check_config_consistency.py --chapter ch06` 后 reseed（config 已增 `passion_seeds`、few-shot 已改，DB active 须同步）。
- steps.py：补 `_is_ch06_step` + `_normalize_ch06_run_output` + `_validate_ch06_output`（commentary readonly / `likes` 嵌套对象数组 / `locked` 布尔 / `locked` 项 `preserve_output` 原位保留）；对 `/run`、`/output`、`/submit` 三路径校验。
- assembler：补 ch06 step-2 `reference_talents` + `passion_seeds` 显式合并块（镜像 ch05 `_ch05_step2_candidates` + `[CH06 STEP-2 CONTRACT]`）；去重规则 = 可见文本 `trim` + 连续空白归一化 + 精确去重；合并产物只进本次 Prompt，不落库额外字段。

**前端层**
- 新增 `renderCh06StepBody`（q1–q5 引导、100 例抽屉、30 问抽屉、关联 ch5 talents 面板、step-1 领域级 likes 编辑、step-2 field/aspects/linked_strengths/sources/locked 卡片、locked 原位保留、commentary Markdown、图 6-1、导出）。
- `_build_ui_context()` 扩展为配置声明驱动白名单，支持 `strength_examples` + `passion_examples`。
- 草稿真实持久化（q1–q5、`reference_talents`、`passion_seeds`、卡片编辑中内容）+ 保存失败提示 + 刷新/重入恢复。
- ch5 缺失时「置灰 + 引导卡」降级态；整体导出 UI（与 ch05 说明书导出一并规划）。

**测试层**
- ch06 专项契约测试：`passion_seeds` 合并、状态机 409、`passion_examples` 载体、mentor submitted-only 反向测试（draft/saved 不进导师）、嵌套 `likes` canonical 唯一。
- 真实浏览器 E2E 通过后，方可宣布 ch06 完成。

---

## 6. 仍开放 / 未变点

- **30 问题库服务**：运行时由题库服务接入 `问题清单.md`；权威源待接，原型 demo 数组演示。
- **ch06 上游门禁（ch07 侧）**：已在 ch07 设计阶段收口（ch07 读 submitted 的 step-2 `likes`），本次无需在 ch06 侧扩大范围。
- **整体导出形态**：与 ch05 一并规划，《我的喜欢的事清单.md》（领域 × 方面 × 关联擅长）。
- 开发侧 §16 验收勾选：在第 4 项（100 例 seed 载体）与第 5 项（ch04 可选依赖）已因本次决策变为「载体=passion_seeds、work_purpose=已移除」，请按本回复更新勾选项。

---

## 7. 一句话总结

开发侧复核扎实、方向正确；设计侧补两处自身文档/配置 bug（canonical 形态、few-shot），并拍板三处待决项（`passion_seeds` 载体、移除 `work_purpose` 契约、30 问接入方式）。**唯一需开发侧特别注意的纠正：ch07 已消费 ch06 `likes`，请勿按旧稿重复添加该依赖。**
